<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>Gab Programming Language – </title>
    <link>/docs/</link>
    <description>Recent content on Gab Programming Language</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en</language>
    <lastBuildDate>Mon, 20 Apr 2026 10:00:44 -0400</lastBuildDate>
    
	  <atom:link href="/docs/index.xml" rel="self" type="application/rss+xml" />
    
    
      
        
      
    
    
    <item>
      <title>Quirks</title>
      <link>/docs/gabonomicon/quirks/</link>
      <pubDate>Mon, 20 Apr 2026 10:00:44 -0400</pubDate>
      
      <guid>/docs/gabonomicon/quirks/</guid>
      <description>
        
        
        &lt;p&gt;Gab is a quirky language. It is probably similar to languages you&amp;rsquo;ve used in the past - but no matter where you come from (Python, Ruby, Elixir, Clojure, even Smalltalk),
you should see some similarities and some funny-looking differences. Lets take a glance at some of the things you may find a bit weird.&lt;/p&gt;
&lt;h2&gt;Imports and Modules&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;imports-and-modules&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#imports-and-modules&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;To run code from another file, send the &lt;code&gt;use:&lt;/code&gt; message to a string literal:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
&#34;github.com/gab-langue/cgab@0.0.5&#34; .use
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The string literal receiver here is the &lt;em&gt;package&lt;/em&gt; name. On the right side of the &lt;code&gt;.use&lt;/code&gt;, you can optionally include a &lt;em&gt;module&lt;/em&gt; name. This looks for a specific
module within the given package. This is how we imported the builtin &lt;code&gt;Io&lt;/code&gt; module earlier!&lt;/p&gt;
&lt;p&gt;Gab searches known locations for a matching package folder. If the package is found, Gab searches the package for a matching module, loads it, and executes it.
If no module was specified, Gab searches for the default &lt;code&gt;mod.gab&lt;/code&gt; module, and executes it.&lt;/p&gt;
&lt;h2&gt;Messages&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;messages&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#messages&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;In Gab, the only way to &lt;em&gt;do&lt;/em&gt; anything is to send a message. This looks like a method call or an operator.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
1 + 2
# :: 3

4 .+ 5
# :: 20
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Even control flow uses messages. You’ll learn more about &lt;code&gt;defcase&lt;/code&gt;, &lt;code&gt;then:&lt;/code&gt;, and &lt;code&gt;else:&lt;/code&gt; later. Until then, here is a peek:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
true: .then () :: do
  &#34;It’s true!&#34; .println
end
# :: It’s true!
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Messages are also &lt;em&gt;values&lt;/em&gt; in Gab. The funky syntax you see for &lt;code&gt;true:&lt;/code&gt; above is actually a &lt;em&gt;message value&lt;/em&gt;. This is how Gab implements, booleans and nil/null.&lt;/p&gt;
&lt;h2&gt;Immutability&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;immutability&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#immutability&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Gab values are immutable. To update a list value, send a message like &lt;code&gt;push:&lt;/code&gt; and begin using the result. In the below example, &lt;code&gt;push:&lt;/code&gt; returns a &lt;em&gt;new&lt;/em&gt; list with the number 4 appended to the end.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
x := [1 2 3]
y := x.push(4)

x.println # :: [1, 2, 3]
y.println # :: [1, 2, 3, 4]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Immutability forces programmers to write code which operates on values instead of state. This actually makes programming easier!&lt;/p&gt;
&lt;h2&gt;Parallelism&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;parallelism&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#parallelism&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Fibers are Gab’s lightweight, isolated threads of execution. You can spawn one with &lt;code&gt;Fibers.make&lt;/code&gt;:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
Fibers.make () :: do
  &#34;Running in a fiber!&#34; .println
end
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Fibers run cooperatively, and in parallel. That means long-running computations occasionally yield control to others. For example, at &lt;code&gt;Io&lt;/code&gt; or &lt;code&gt;Channel&lt;/code&gt; operations.&lt;/p&gt;
&lt;h2&gt;Communication&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;communication&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#communication&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Speaking of channels, lets talk about how fibers can talk to each other.&lt;/p&gt;
&lt;p&gt;Channels let fibers exchange data. Create one with &lt;code&gt;Channels.make&lt;/code&gt;, then use the send &lt;code&gt;&amp;lt;!&lt;/code&gt; and receive &lt;code&gt;&amp;gt;!&lt;/code&gt; messages:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
ch := Channels.make

Fibers.make () :: do
  ch &lt;! &#34;ping&#34;
end

msg := ch &gt;!
msg.println
# :: ping
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Channels are &lt;strong&gt;unbuffered&lt;/strong&gt; and &lt;strong&gt;rendezvous-based&lt;/strong&gt; — both sides must meet for a transfer to occur.&lt;/p&gt;
&lt;p&gt;Channels can be closed with &lt;code&gt;close:&lt;/code&gt; to cancel current operations and prevent future ones.&lt;/p&gt;
&lt;h2&gt;Putting It All Together&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;putting-it-all-together&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#putting-it-all-together&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Let’s combine what we’ve learned into a simple producer-consumer example. This is a common pattern you&amp;rsquo;ll use in Gab programs.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
# Imports excluded for brevity.
ch := Channels.make

# The producer iterates the range 1-5 and puts each number on the channel with a little tag (the num: message)
producer := Fibers.make () :: do
  Ranges.make(1 5).each (i) :: do
    ch &lt;! (num: i)
  end

  ch.close
end

# The consumer then loops forever (calling itself). On each iteration, it takes from the channel
# checks the status of the take, and then prints the result.
consumer := Fibers.make () :: do
  loop := () :: do
    (status, kind, value) := ch &gt;!

    status.ok.then () :: do
      &#34;Received: $ $&#34;.sprintf(kind, value).println
    end

    (status.ok &amp; ch.is\open).then self
  end

  loop.()
end

# Map over a list consisting of the producer and consumer fibers, awaiting each of them.
# This prevents the program from terminating before the fibers are done producing and consuming.
[producer, consumer].each f :: f.await
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;That’s it — you’ve written your first Gab program, used the REPL, spawned fibers, and passed messages through channels.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title>Key-Value Store</title>
      <link>/docs/gabonomicon/examples/kv_store/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/gabonomicon/examples/kv_store/</guid>
      <description>
        
        
        &lt;p&gt;This example builds an in-memory key-value store. It is a complete, useful program and demonstrates how Gab&amp;rsquo;s core features compose in practice.&lt;/p&gt;
&lt;p&gt;By the end you&amp;rsquo;ll have a store that can be safely read and written from any number of fibers.&lt;/p&gt;
&lt;h2&gt;The Design&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;the-design&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#the-design&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The store is a single fiber. It owns a record which it privately threads through recursive calls. No other fiber can touch that record directly.&lt;/p&gt;
&lt;p&gt;The store&amp;rsquo;s public face is a &lt;strong&gt;channel&lt;/strong&gt;. Callers send command tuples into it:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;The store fiber reads commands off the channel&lt;/li&gt;
&lt;li&gt;It processes each one&lt;/li&gt;
&lt;li&gt;It then sends the result back on the reply channel included in the command&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The command name comes first in the tuple so it can be forwarded directly as the receiver of &lt;code&gt;handle:&lt;/code&gt;.&lt;/p&gt;
&lt;div class=&#34;hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code&#34;&gt;

&lt;div&gt;&lt;pre&gt;&lt;code&gt;caller fiber                   store fiber
    |                               |
    |-- (store\set: reply k v) ---------&amp;gt; ch
    |                               |-- state := state.put(k, v)
    |                               |-- reply &amp;lt;! ok:
    | &amp;lt;----------- ok: ------------ |&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;div class=&#34;hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0&#34;&gt;
  &lt;button
    class=&#34;hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50&#34;
    title=&#34;Copy code&#34;
    aria-label=&#34;Copy code&#34;
    data-copied-label=&#34;Copied!&#34;
  &gt;
    &lt;div class=&#34;hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
&lt;div class=&#34;hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
  &lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;Because all state lives inside one fiber, parallel callers are automatically serialised. The design makes races structurally impossible.&lt;/p&gt;
&lt;h2&gt;The Store Actor&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;the-store-actor&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#the-store-actor&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;&lt;code&gt;make:&lt;/code&gt; creates a channel, spawns the store fiber, and returns the channel directly. The store &lt;strong&gt;is&lt;/strong&gt; the channel. Callers hold a channel value and send commands into it.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
Stores := store:

make: .def (Stores, () :: do
  ch := Channels.make

  loop := (state) :: do
    (cmd, reply, args*) := ch &gt;! .unwrap
    next_state := (cmd, reply, state, args*) .handle
    self.(next_state)
  end

  Fibers.make () :: loop.({})

  ch
end)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The fiber loops by recursing with &lt;code&gt;self.(next_state)&lt;/code&gt;. &lt;code&gt;self&lt;/code&gt; is the &lt;code&gt;loop&lt;/code&gt; block receiving the empty message. Each iteration receives the next command with &lt;code&gt;ch &amp;gt;! .unwrap&lt;/code&gt;, destructures it, injects &lt;code&gt;state&lt;/code&gt; after &lt;code&gt;reply&lt;/code&gt;, then forwards the whole tuple to &lt;code&gt;handle:&lt;/code&gt;. Because &lt;code&gt;cmd&lt;/code&gt; is first, it becomes the receiver of &lt;code&gt;handle:&lt;/code&gt; naturally via tuple forwarding — no explicit dispatch required.&lt;/p&gt;
&lt;p&gt;The command handlers each receive &lt;code&gt;(reply, state, args*)&lt;/code&gt; and return the next state:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
handle: .defcase {
  store\get: (reply, state, k) :: do
    reply &lt;! (state.at k)
    state
  end

  store\set: (reply, state, k, v) :: do
    reply &lt;! ok:
    state.put(k, v)
  end

  store\delete: (reply, state, k) :: do
    reply &lt;! (state.at k)
    state.take(k)
  end
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;store\get:&lt;/code&gt; forwards the result of &lt;code&gt;state.at&lt;/code&gt; directly — if the key exists the caller receives &lt;code&gt;(ok: value)&lt;/code&gt;, otherwise &lt;code&gt;(none: nil:)&lt;/code&gt;. &lt;code&gt;store\set:&lt;/code&gt; replies with &lt;code&gt;ok:&lt;/code&gt; and returns the updated state. &lt;code&gt;store\delete:&lt;/code&gt; replies with the same result as &lt;code&gt;at&lt;/code&gt; — telling the caller what was there — then returns the state with the key removed via &lt;code&gt;take:&lt;/code&gt;.&lt;/p&gt;
&lt;h2&gt;Clean up the api&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;clean-up-the-api&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#clean-up-the-api&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The module is defined on the channel type directly, since the store is a channel:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
# Define the t: message on our Store module, as is convention.
# Because a store *is* a channel, we&#39;ll use Channels.t as the type.
t: .def (Stores, Channels.t)

# Define messages on our Store.t
[Stores.t] .defmodule {
  store\get: (k) :: do
    reply := Channels.make
    self &lt;! (store\get: reply k)
    reply &gt;! .unwrap
  end

  store\set: (k, v) :: do
    reply := Channels.make
    self &lt;! (store\set: reply k v)
    reply &gt;! .unwrap
  end

  store\delete: (k) :: do
    reply := Channels.make
    self &lt;! (store\delete: reply k)
    reply &gt;! .unwrap
  end
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;defmodule&lt;/code&gt; attaches messages directly to the &lt;code&gt;Store.t&lt;/code&gt; type, so &lt;code&gt;self&lt;/code&gt; inside each method is the channel itself. Each method creates a reply channel, sends the command, and blocks on &lt;code&gt;reply &amp;gt;! .unwrap&lt;/code&gt; until the store responds.&lt;/p&gt;
&lt;h2&gt;Putting it together&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;putting-it-together&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#putting-it-together&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Here is an example of how we&amp;rsquo;d expect to use this api:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
store := Stores.make

# Set some values
store.store\set(&#39;name&#39;, &#39;Gab&#39;)
store.store\set(&#39;version&#39;, &#39;0.1.1&#39;)

# Get a value
store.store\get(&#39;name&#39;)
  .then((val) :: &#39;name is: $&#39;.sprintf(val).println)
  .else(()    :: &#39;not found&#39;.println)
# :: name is: Gab

# Overwrite
store.store\set(&#39;name&#39;, &#39;cgab&#39;)
store.store\get(&#39;name&#39;)
  .then((val) :: val.println)
# :: cgab

# Delete
store.store\delete(&#39;version&#39;)
store.store\get(&#39;version&#39;)
  .else(() :: &#39;not found&#39;.println)
# :: not found

# Missing key
store.store\get(&#39;missing&#39;)
  .else(() :: &#39;not found&#39;.println)
# :: not found
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Parallel access&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;parallel-access&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#parallel-access&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The store is safe to use from any number of fibers simultaneously. Because all state is owned by the store fiber, and all access goes through the channel, operations are automatically serialised:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
store := Stores.make

fibers := Ranges.make(0, 1000).map (i) :: do
  Fibers.make () :: do
    key := &#39;fiber-$&#39;.sprintf(i)
    store.store\set(key, i)
    store.store\get(key)
      .then((val) :: &#39;$: $&#39;.sprintf(key, val).println)
  end
end

fibers.each f :: f.await
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The store is the only path to the state. Serialisation is a consequence of the topology, not something enforced by the programmer.&lt;/p&gt;
&lt;h2&gt;The full program&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;the-full-program&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#the-full-program&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Here is a final, full example of our key-value store.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
Stores := store:

make: .def (Stores, () :: do
  ch := Channels.make

  loop := (state) :: do
    (cmd, reply, args*) := ch &gt;! .unwrap
    next_state := (cmd, reply, state, args*) .handle
    self.(next_state)
  end

  Fibers.make () :: loop.({})

  ch
end)

handle: .defcase {
  store\get: (reply, state, k) :: do
    reply &lt;! (state.at k)
    state
  end

  store\set: (reply, state, k, v) :: do
    reply &lt;! ok:
    state.put(k, v)
  end

  store\delete: (reply, state, k) :: do
    reply &lt;! (state.at k)
    state.take(k)
  end
}

t: .def (Stores, Channels.t)

[Stores.t] .defmodule {
  store\get: (k) :: do
    reply := Channels.make
    self &lt;! (store\get: reply k)
    reply &gt;! .unwrap
  end

  store\set: (k, v) :: do
    reply := Channels.make
    self &lt;! (store\set: reply k v)
    reply &gt;! .unwrap
  end

  store\delete: (k) :: do
    reply := Channels.make
    self &lt;! (store\delete: reply k)
    reply &gt;! .unwrap
  end
}

Stores
&lt;/code&gt;&lt;/pre&gt;

      </description>
    </item>
    
    <item>
      <title>Messages</title>
      <link>/docs/gabonomicon/tour/messages/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/gabonomicon/tour/messages/</guid>
      <description>
        
        
        &lt;p&gt;In most languages, control flow is driven by keywords: &lt;code&gt;if&lt;/code&gt;, &lt;code&gt;for&lt;/code&gt;, &lt;code&gt;while&lt;/code&gt;, &lt;code&gt;switch&lt;/code&gt;. Gab has none of these. Instead, everything is accomplished by &lt;strong&gt;sending messages to values&lt;/strong&gt;.&lt;/p&gt;
&lt;p&gt;If you&amp;rsquo;ve used Ruby or Smalltalk, this will feel familiar. If you haven&amp;rsquo;t, don&amp;rsquo;t worry — it&amp;rsquo;s a simple idea with surprisingly powerful implications.&lt;/p&gt;
&lt;h2&gt;Sending a message&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;sending-a-message&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#sending-a-message&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Sending messages to values is the foundation of Gab programs. It looks just like a method call:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
&#39;hello&#39;.println
# :: hello

[&#39;cat&#39;, &#39;dog&#39;, &#39;bird&#39;].len
# :: 3
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The &lt;strong&gt;value&lt;/strong&gt; on the left receives the &lt;strong&gt;message&lt;/strong&gt; on the right. The message may produce a result, which you can immediately send another message to. This is called &lt;strong&gt;chaining&lt;/strong&gt;:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
[&#39;Hello&#39;, &#39; &#39;, &#39;world!&#39;].join.println
# :: Hello world!
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Two forms of message send&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;two-forms-of-message-send&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#two-forms-of-message-send&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;There are exactly two kinds of message send in Gab.&lt;/p&gt;
&lt;p&gt;A &lt;strong&gt;named send&lt;/strong&gt; is a &lt;code&gt;.&lt;/code&gt; followed by one or more letters, underscores, or numbers:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
&#39;hello&#39;.println
[&#39;cat&#39;, &#39;dog&#39;, &#39;bird&#39;].len
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;An &lt;strong&gt;operator send&lt;/strong&gt; uses a sequence of operator characters (&lt;code&gt;+&lt;/code&gt;, &lt;code&gt;-&lt;/code&gt;, &lt;code&gt;*&lt;/code&gt;, &lt;code&gt;/&lt;/code&gt;, &lt;code&gt;&amp;lt;&lt;/code&gt;, &lt;code&gt;&amp;gt;&lt;/code&gt;, &lt;code&gt;!&lt;/code&gt;, &lt;code&gt;&amp;lt;!&lt;/code&gt;, and others):&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
10 + 5      # :: 15
channel &lt;! &#39;value&#39;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;That&amp;rsquo;s the complete syntax for invoking behaviour. There is no special function-call form, and no keywords for control flow.&lt;/p&gt;
&lt;h2&gt;Messages with arguments&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;messages-with-arguments&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#messages-with-arguments&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Like the operators above, some messages may take arguments. Arguments are whatever you put on the right side of the send.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
&#39;Hello, $!&#39; .sprintf &#39;world&#39; .println
# :: Hello, world
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Here, &lt;code&gt;sprintf&lt;/code&gt; is a message sent to the string &lt;code&gt;&#39;Hello, $!&#39;&lt;/code&gt;. It replaces each &lt;code&gt;$&lt;/code&gt; in the string with the corresponding arguments. The result is then sent the &lt;code&gt;println&lt;/code&gt; message.&lt;/p&gt;
&lt;p&gt;You can pass multiple arguments by wrapping consecutive expressions in &lt;code&gt;()&lt;/code&gt;, called a &lt;em&gt;tuple&lt;/em&gt;.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
result := Strings.make(&#39;Hello&#39; &#39;, &#39; &#39;world!&#39;)
result.println
# :: Hello, world!
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Tuples&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;tuples&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#tuples&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;I&amp;rsquo;ve slightly lied to you! Gab actually isn&amp;rsquo;t strict about what &lt;em&gt;side&lt;/em&gt; of the send you put the arguments on. Gab implicitly collects the left and right sides into
a singular &lt;strong&gt;tuple&lt;/strong&gt;, and sends the message to &lt;strong&gt;that tuple&lt;/strong&gt;. Therefore, you could rewrite:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
1 + 1
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;as:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
(1 1) +
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;These are semantically identical in Gab. Tuples will be important later on, and help to make Gab efficient.&lt;/p&gt;
&lt;h2&gt;Defining new messages&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;defining-new-messages&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#defining-new-messages&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Message values are just the name of the message written with a trailing colon: &lt;code&gt;greet:&lt;/code&gt;, &lt;code&gt;println:&lt;/code&gt;, &lt;code&gt;def:&lt;/code&gt;, &lt;code&gt;+:&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;You can define a new message &lt;em&gt;specialization&lt;/em&gt; by sending the &lt;code&gt;def:&lt;/code&gt; message to a message value:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
greet: .def (Strings.t, () :: do
  &#39;Hello, $!!&#39;.sprintf(self).println
end)

&#39;Alice&#39;.greet
# :: Hello, Alice!
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;def:&lt;/code&gt; takes two arguments: the &lt;strong&gt;receiver type&lt;/strong&gt; and a &lt;strong&gt;block&lt;/strong&gt; containing the implementation. Here, &lt;code&gt;Strings.t&lt;/code&gt; is the conventional way to refer to the string type — it is a message sent to the &lt;code&gt;Strings&lt;/code&gt; module that returns the type upon which new string messages should be defined. Using &lt;code&gt;Strings.t&lt;/code&gt; rather than a bare type name is a convention you&amp;rsquo;ll see throughout Gab&amp;rsquo;s standard library, and one you should follow in your own modules.&lt;/p&gt;
&lt;p&gt;Inside the block, &lt;code&gt;self&lt;/code&gt; refers to the value that received the message.&lt;/p&gt;
&lt;p&gt;Message definitions are &lt;strong&gt;specializations&lt;/strong&gt; — the same message name can have completely different implementations depending on the type of the receiver.
This is how Gab achieves polymorphism without classes or interfaces.
More on this in the &lt;a href=&#34;/docs/gabonomicon/tour/records_and_shapes/&#34;&gt;Records &amp;amp; Shapes&lt;/a&gt; section.&lt;/p&gt;
&lt;h2&gt;Control flow with messages&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;control-flow-with-messages&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#control-flow-with-messages&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Because Gab has no &lt;code&gt;if&lt;/code&gt; keyword, conditional branching is done by sending messages. Boolean values respond to messages like &lt;code&gt;then:&lt;/code&gt; and &lt;code&gt;else:&lt;/code&gt;:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
age := 20

(age &gt; 18)
  .then(() :: &#39;You may enter.&#39;.println)
  .else(() :: &#39;Come back in a few years.&#39;.println)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;At first this looks unfamiliar, but it&amp;rsquo;s the same idea: send a message to a value (the boolean result of &lt;code&gt;age &amp;gt; 18&lt;/code&gt;), and pass blocks as arguments for each branch. The boolean decides which block to invoke.&lt;/p&gt;
&lt;p&gt;This means branching, looping, and every other form of control flow are just messages — there&amp;rsquo;s nothing special about them syntactically.&lt;/p&gt;
&lt;h2&gt;Why?&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;why&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#why&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Constraining the language to a single dispatch mechanism is a deliberate trade-off. You give up some familiarity, and in return you get:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Consistency.&lt;/strong&gt; There&amp;rsquo;s one way to invoke behaviour. No need to distinguish between method calls, function calls, operators, and special forms.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Extensibility.&lt;/strong&gt; You can define new messages for any type, including built-in types, without modifying their source.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Performance.&lt;/strong&gt; With a single dispatch path, the runtime can focus all optimization work on making message sends fast.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Composability.&lt;/strong&gt; The fewer language features exist, the more our code &lt;em&gt;accidently&lt;/em&gt; composes with itself and others.&lt;/li&gt;
&lt;/ul&gt;

      </description>
    </item>
    
    <item>
      <title>Numbers</title>
      <link>/docs/gabonomicon/basic_types/arithmetic/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/gabonomicon/basic_types/arithmetic/</guid>
      <description>
        
        
        &lt;p&gt;All numbers in Gab are IEEE 754 64-bit floating point values. There is no distinct integer type.&lt;/p&gt;
&lt;h2&gt;&lt;code&gt;gab\number&lt;/code&gt;&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;gabnumber&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#gabnumber&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Numbers literals may have leading &lt;code&gt;-&lt;/code&gt;, or even be written with exponents, or in hex.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
1
-42
1.2e4
0.2

3.14?   # :: gab\number
&lt;/code&gt;&lt;/pre&gt;
&lt;blockquote&gt;
  &lt;p&gt;Numbers less than one require a leading zero: &lt;code&gt;0.5&lt;/code&gt;, not &lt;code&gt;.5&lt;/code&gt;. A bare &lt;code&gt;.&lt;/code&gt; is the empty message send in Gab.&lt;/p&gt;

&lt;/blockquote&gt;
&lt;h2&gt;Arithmetic&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;arithmetic&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#arithmetic&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The standard arithmetic operators are all message sends:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
10 + 3   # :: 13
10 - 3   # :: 7
10 * 3   # :: 30
10 / 3   # :: 3.33333
10 % 3   # :: 1
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Bitwise operations and 53-bit integers&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;bitwise-operations-and-53-bit-integers&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#bitwise-operations-and-53-bit-integers&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;For bitwise operations, Gab uses &lt;strong&gt;53-bit integers&lt;/strong&gt;. This is because 64-bit floats can only represent integers losslessly up to 2^52. Limiting integers to 52 bits guarantees that Gab can convert freely between its float representation and integer operations without loss.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
1 &lt;&lt; 52   # :: -4.5036e+15
1 &lt;&lt; 53   # :: 0
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The standard bitwise operators:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
4 &amp; 6    # :: 4   (AND)
4 | 2    # :: 6   (OR)
4 &lt;&lt; 1   # :: 8   (left shift)
4 &gt;&gt; 1   # :: 2   (right shift)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Bit-shifting edge cases&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;bit-shifting-edge-cases&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#bit-shifting-edge-cases&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Gab defines behaviour for several cases that are undefined or implementation-defined in C.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Shifting by a negative amount&lt;/strong&gt; is treated as a shift in the opposite direction:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
4 &gt;&gt; -1   # :: 8
4 &lt;&lt; -1   # :: 2
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Shifting by more than 52&lt;/strong&gt; returns zero:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
4 &gt;&gt; 65   # :: 0
4 &lt;&lt; 65   # :: 0
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Shifting negative integers&lt;/strong&gt; follows Lua&amp;rsquo;s semantics: the shift is performed on the unsigned bit representation, then converted back. This means right-shifting a negative number does not preserve the sign bit — it produces a large positive number:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
-4 &gt;&gt; 1   # :: 9223372036854775806
-4 &lt;&lt; 1   # :: -8
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This is asymmetric but is the most efficient implementation, and shifting negative integers is uncommon enough that the trade-off is worthwhile.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title>Networked Key-Value Store</title>
      <link>/docs/gabonomicon/examples/networked_kv_store/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/gabonomicon/examples/networked_kv_store/</guid>
      <description>
        
        
        &lt;p&gt;This example extends the &lt;a href=&#34;/docs/gabonomicon/examples/kv_store/&#34;&gt;in-memory key-value store&lt;/a&gt; with a TCP server. Clients connect and send text commands; the server parses each line, dispatches it to the store actor, and writes a response.&lt;/p&gt;
&lt;h2&gt;The Protocol&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;the-protocol&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#the-protocol&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Each request is a single newline-terminated line. Each response is a single newline-terminated line.&lt;/p&gt;
&lt;table&gt;
  &lt;thead&gt;
      &lt;tr&gt;
          &lt;th&gt;Request&lt;/th&gt;
          &lt;th&gt;Response&lt;/th&gt;
          &lt;th&gt;No key response&lt;/th&gt;
      &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;code&gt;GET key&lt;/code&gt;&lt;/td&gt;
          &lt;td&gt;&lt;code&gt;OK value&lt;/code&gt;&lt;/td&gt;
          &lt;td&gt;&lt;code&gt;NONE&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;code&gt;SET key value&lt;/code&gt;&lt;/td&gt;
          &lt;td&gt;&lt;code&gt;OK&lt;/code&gt;&lt;/td&gt;
          &lt;td&gt;—&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;&lt;code&gt;DELETE key&lt;/code&gt;&lt;/td&gt;
          &lt;td&gt;&lt;code&gt;OK value&lt;/code&gt;&lt;/td&gt;
          &lt;td&gt;&lt;code&gt;NONE&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;
&lt;hr&gt;
&lt;h2&gt;Handling a Connection&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;handling-a-connection&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#handling-a-connection&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Each accepted connection gets its own fiber. The fiber reads one line, dispatches to the store, writes the response, and recurses until the client disconnects:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
serve_client: .def (IO.Sockets.t, (store) :: do
  sock := self

  &#39;Connected $&#39;.sprintf(sock).println

  loop := () :: do
    recurse := self

    sock
      .until(&#39;\n&#39;.to\b)
      .then(line :: do
        (cmd, args*) := line.as\string.unwrap.trim.split(&#39; &#39;)
        response := cmd.to\message.run_command(sock, store, args*)
        sock.write(&#39;$\n&#39;.sprintf(response).to\binary)
        recurse.()
      end)
      .else(() :: do
        &#39;Disconnected $&#39;.sprintf(sock).println
      end)
  end

  loop.()
end)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Note that &lt;code&gt;self&lt;/code&gt; is captured as &lt;code&gt;sock&lt;/code&gt; immediately. Inside the nested &lt;code&gt;then:&lt;/code&gt; block, &lt;code&gt;self&lt;/code&gt; would refer to the block rather than the socket. Similarly, &lt;code&gt;recurse := self&lt;/code&gt; inside &lt;code&gt;loop&lt;/code&gt; captures the &lt;code&gt;loop&lt;/code&gt; block so the &lt;code&gt;then:&lt;/code&gt; branch can recurse after writing the response.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;sock.until(&#39;\n&#39;.to\b)&lt;/code&gt; blocks until a newline arrives or the connection closes. On success, the line is converted from binary to string, trimmed, and split on spaces.&lt;code&gt;cmd&lt;/code&gt; takes the first token, &lt;code&gt;args*&lt;/code&gt; collects the rest. &lt;code&gt;cmd.to\m&lt;/code&gt; converts the command string to a message value, which dispatches through &lt;code&gt;run_command:&lt;/code&gt;. On failure the disconnection is logged and the fiber exits.&lt;/p&gt;
&lt;p&gt;The command handlers receive the socket, the store, and any remaining arguments:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
run_command: .defcase {
  GET: (socket, store, key, rest*) :: do
    store.store\get key
      .then((val) :: &#39;OK $&#39;.sprintf(val))
      .else(()    :: &#39;NONE&#39;)
  end

  SET: (socket, store, key, val, rest*) :: do
    store.store\set (key val)
      .then(() :: &#39;OK&#39;)
  end

  DELETE: (socket, store, key, rest*) :: do
    store.store\delete key
      .then((val) :: &#39;OK $&#39;.sprintf(val))
      .else(()    :: &#39;NONE&#39;)
  end
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The &lt;code&gt;defcase&lt;/code&gt; keys are &lt;code&gt;GET:&lt;/code&gt;, &lt;code&gt;SET:&lt;/code&gt;, and &lt;code&gt;DELETE:&lt;/code&gt;. They explicitly match what &lt;code&gt;to\m&lt;/code&gt; produces from the wire protocol strings. &lt;code&gt;rest*&lt;/code&gt; absorbs any extra tokens so malformed commands don&amp;rsquo;t crash the handler. Missing arguments arrive as &lt;code&gt;nil:&lt;/code&gt;, which the store returns &lt;code&gt;none:&lt;/code&gt; for, propagating back to the client as &lt;code&gt;NONE&lt;/code&gt;.&lt;/p&gt;
&lt;h2&gt;The Accept Loop&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;the-accept-loop&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#the-accept-loop&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The server accepts connections one at a time, immediately spawning a fiber for each and looping:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
accept_loop: .def (IO.Sockets.t, (store) :: do
  server := self

  self.accept
    .then((client) :: do
      Fibers.make () :: client.serve_client(store)
      server.accept_loop(store)
    end)
    .else(() :: &#39;server closed&#39;.println)
end)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;server := self&lt;/code&gt; captures the socket before the &lt;code&gt;then:&lt;/code&gt; block, where &lt;code&gt;self&lt;/code&gt; would refer to the block. &lt;code&gt;accept&lt;/code&gt; blocks until a client connects; a new fiber is spawned immediately and the loop recurses without waiting for that client to finish.&lt;/p&gt;
&lt;h2&gt;Starting the Server&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;starting-the-server&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#starting-the-server&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;&lt;code&gt;start:&lt;/code&gt; is defined on the store type. It creates the socket, binds, listens, and launches the accept loop in a fiber:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
[Store.t] .defmodule {
  start: (host, port) :: do
    server := Socket.make(tcp:).unwrap
    server.bind(host port).unwrap
    server.listen(128).unwrap
    &#39;Listening on $:$&#39;.sprintf(host port).println
    store := self
    Fibers.make () :: server.accept_loop(store)
  end
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Inside the fiber, &lt;code&gt;self&lt;/code&gt; would refer to the fiber&amp;rsquo;s own block. Because of this, we capture the &lt;code&gt;self&lt;/code&gt; as &lt;code&gt;store&lt;/code&gt; beforehand.&lt;/p&gt;
&lt;h2&gt;Putting it Together&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;putting-it-together&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#putting-it-together&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Here is how you can now use the store:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
store  := Store.make
server := store.start(&#39;::1&#39; 6379)
server.await
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Connect with any TCP client:&lt;/p&gt;
&lt;div class=&#34;hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code&#34;&gt;

&lt;div&gt;&lt;div class=&#34;highlight&#34;&gt;&lt;pre tabindex=&#34;0&#34; style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;&#34;&gt;&lt;code class=&#34;language-sh&#34; data-lang=&#34;sh&#34;&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;$ echo &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;SET name Gab&amp;#34;&lt;/span&gt; | nc ::1 &lt;span style=&#34;color:#ae81ff&#34;&gt;6379&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;OK
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;$ echo &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;GET name&amp;#34;&lt;/span&gt; | nc ::1 &lt;span style=&#34;color:#ae81ff&#34;&gt;6379&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;OK Gab
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;$ echo &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;DELETE name&amp;#34;&lt;/span&gt; | nc ::1 &lt;span style=&#34;color:#ae81ff&#34;&gt;6379&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;OK Gab
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;$ echo &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;GET name&amp;#34;&lt;/span&gt; | nc ::1 &lt;span style=&#34;color:#ae81ff&#34;&gt;6379&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;NONE&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&#34;hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0&#34;&gt;
  &lt;button
    class=&#34;hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50&#34;
    title=&#34;Copy code&#34;
    aria-label=&#34;Copy code&#34;
    data-copied-label=&#34;Copied!&#34;
  &gt;
    &lt;div class=&#34;hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
&lt;div class=&#34;hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
  &lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;h2&gt;The Full Program&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;the-full-program&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#the-full-program&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The full store module now looks like this:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
Socket := IO.Sockets
Store  := store:

# --- Store actor ---

make: .def (Store, () :: do
  ch := Channels.make

  loop := (state) :: do
    (cmd, reply, args*) := ch &gt;! .unwrap
    next_state := (cmd, reply, state, args*) .handle
    self.(next_state)
  end

  Fibers.make () :: loop.({})

  ch
end)

handle: .defcase {
  store\get: (reply, state, k) :: do
    reply &lt;! (state.at k)
    state
  end

  store\set: (reply, state, k, v) :: do
    reply &lt;! ok:
    state.put(k, v)
  end

  store\delete: (reply, state, k) :: do
    reply &lt;! (state.at k)
    state.take(k)
  end
}

t: .def (Store, Channels.t)

[Store.t] .defmodule {
  store\get: (k) :: do
    reply := Channels.make
    self &lt;! (store\get: reply k)
    reply &gt;! .unwrap
  end

  store\set: (k, v) :: do
    reply := Channels.make
    self &lt;! (store\set: reply k v)
    reply &gt;! .unwrap
  end

  store\delete: (k) :: do
    reply := Channels.make
    self &lt;! (store\delete: reply k)
    reply &gt;! .unwrap
  end

  start: (host, port) :: do
    server := Socket.make(tcp:).unwrap
    server.bind(host port).unwrap
    server.listen(128).unwrap
    &#39;Listing on $:$&#39;.sprintf(host port).println
    store := self
    Fibers.make () :: server.accept_loop(store)
  end
}

run_command: .defcase {
  GET: (socket, store, key, rest*) :: do
    store.store\get key
      .then((val) :: &#39;OK $&#39;.sprintf(val))
      .else(()    :: &#39;NONE&#39;)
  end

  SET: (socket, store, key, val, rest*) :: do
    store.store\set (key val)
      .then(() :: &#39;OK&#39;)
  end

  DELETE: (socket, store, key, rest*) :: do
    store.store\delete key
      .then((val) :: &#39;OK $&#39;.sprintf(val))
      .else(()    :: &#39;NONE&#39;)
  end
}

serve_client: .def (IO.Sockets.t, (store) :: do
  sock := self

  &#39;Connected $&#39;.sprintf(sock).println

  loop := () :: do
    recurse := self

    sock
      .until(&#39;\n&#39;.to\b)
      .then(line :: do
        (cmd, args*) := line.as\s.unwrap.trim.split(&#39; &#39;)
        response := cmd.to\m.run_command(sock, store, args*)
        sock.write(&#39;$\n&#39;.sprintf(response).to\b)
        recurse.()
      end)
      .else(() :: do
        &#39;Disconnected $&#39;.sprintf(sock).println
      end)
  end

  loop.()
end)

accept_loop: .def (IO.Sockets.t, (store) :: do
  server := self

  self.accept
    .then((client) :: do
      Fibers.make () :: client.serve_client(store)
      server.accept_loop(store)
    end)
    .else(() :: &#39;server closed&#39;.println)
end)

store  := Store.make
server := store.start(&#39;::1&#39; 6379)
server.await
&lt;/code&gt;&lt;/pre&gt;

      </description>
    </item>
    
    <item>
      <title>Records &amp; Shapes</title>
      <link>/docs/gabonomicon/tour/records_and_shapes/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/gabonomicon/tour/records_and_shapes/</guid>
      <description>
        
        
        &lt;h2&gt;Records&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;records&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#records&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Gab has one compound data structure: &lt;code&gt;gab\record&lt;/code&gt;. Both Lists and Dictionaries are built on top of it. This constraint is intentional and furthers Gab&amp;rsquo;s focus on minimalism.&lt;/p&gt;
&lt;h3&gt;Dictionaries&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;dictionaries&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#dictionaries&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;Curly bracket syntax creates dict-style records. The values between the &lt;code&gt;{}&lt;/code&gt; are treated as alternating keys and values.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
bob := { name: &#39;bob&#39;, age: 44 }
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Access values using message sends:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
bob.name  # :: &#39;bob&#39;
bob.age   # :: 44
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;If the message doesn&amp;rsquo;t exist as a key in the record, Gab will panic with &lt;code&gt;MISSING SPECIALIZATION&lt;/code&gt;. If you&amp;rsquo;re not sure the key exists, try:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
(ok, is_hungry) := bob.at(hungry:)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;If the key exists, &lt;code&gt;ok&lt;/code&gt; will be &lt;code&gt;ok:&lt;/code&gt;, and &lt;code&gt;is_hungry&lt;/code&gt; will have the value in the record. Otherwise, ok will be &lt;code&gt;none:&lt;/code&gt;. More on this pattern in the section on &lt;a href=&#34;/docs/gabonomicon/tour/error_handling/&#34;&gt;error handling&lt;/a&gt;.&lt;/p&gt;
&lt;h3&gt;Lists&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;lists&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#lists&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;Square-bracket syntax creates &lt;strong&gt;list-style&lt;/strong&gt; records, where the keys are implicit integer indices:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
colors := [&#39;red&#39;, &#39;green&#39;, &#39;blue&#39;]
colors.at(0).unwrap  # :: &#39;red&#39;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Both &lt;em&gt;Dictionaries&lt;/em&gt; and &lt;em&gt;Lists&lt;/em&gt; share the &lt;code&gt;gab\record&lt;/code&gt; type.&lt;/p&gt;
&lt;h2&gt;Immutability&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;immutability&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#immutability&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;All records in Gab are &lt;strong&gt;immutable&lt;/strong&gt; - You cannot change a record in place. Instead, messages like &lt;code&gt;put&lt;/code&gt; return a new record with the updated value, leaving the original value untouched:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
bob := { name: &#39;bob&#39;, age: 44 }

alice := bob.put(name: &#39;alice&#39;)

bob    # :: { name: &#39;bob&#39;, age: 44 }
alice  # :: { name: &#39;alice&#39;, age: 44 }
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Gab&amp;rsquo;s immutable record is implemented as a Hash-Array-Mapped Trie, a persistent data structure used by many functional languages. &lt;code&gt;put&lt;/code&gt; operations essentially create &lt;strong&gt;diffs&lt;/strong&gt;, which allow records to share the data that didn&amp;rsquo;t change.&lt;/p&gt;
&lt;h2&gt;Shapes&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;shapes&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#shapes&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Records are especially unique for their type. Before we can discuss what this means and why its important, lets get a baseline to compare to.&lt;/p&gt;
&lt;p&gt;Every value in Gab has a &lt;strong&gt;type&lt;/strong&gt;, which you can inspect with the &lt;code&gt;?&lt;/code&gt; message. For most values, &lt;code&gt;?&lt;/code&gt; returns a string describing the type:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
&#39;hello&#39; ?  # :: &#39;gab\string&#39;
44 ?       # :: &#39;gab\number&#39;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Message values are their own type — &lt;code&gt;?&lt;/code&gt; on a message returns the message itself:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
ok: ?  # :: ok:
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;A record&amp;rsquo;s type is its &lt;strong&gt;shape&lt;/strong&gt;. This is a separate value that describes the record&amp;rsquo;s keys:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
{ name: &#39;bob&#39;, age: 44 } ?
# :: &lt;gab\shape name: age:&gt;

[&#39;red&#39;, &#39;green&#39;, &#39;blue&#39;] ?
# :: &lt;gab\shape 0: 1: 2:&gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;All records with the same keys in the same order share the same shape. It follows that any two records with the same shape therefore &lt;em&gt;have the same type&lt;/em&gt;.&lt;/p&gt;
&lt;h3&gt;Defining messages for shapes&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;defining-messages-for-shapes&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#defining-messages-for-shapes&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;This is where shapes become powerful. When you define a message, you specialize it for a particular type. Typically, this is either a builtin type via &lt;code&gt;&amp;lt;Module&amp;gt;.t&lt;/code&gt;, a message,  or a &lt;strong&gt;shape&lt;/strong&gt;.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
# Capture the shape of a &#34;Person-like&#34; record
Person := { name: &#39;bob&#39;, age: 0 } ?
# Or construct a shape from keys directly
Person := Shapes.make(name: age:)

birthday: .def (Person, () :: do
  &#39;Happy Birthday, $!&#39;.sprintf(self.name).println
  self.put(age: self.age + 1)
end)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Here &lt;code&gt;birthday:&lt;/code&gt; is a &lt;strong&gt;message value&lt;/strong&gt; — the trailing colon is part of the value, not punctuation. Sending &lt;code&gt;def:&lt;/code&gt; to &lt;code&gt;birthday:&lt;/code&gt; registers a new specialization: whenever a record with the &lt;code&gt;Person&lt;/code&gt; shape receives &lt;code&gt;birthday:&lt;/code&gt;, call this method.&lt;/p&gt;
&lt;p&gt;It is a convention in Gab to define a &lt;code&gt;t:&lt;/code&gt; message on your own modules that returns the type others should specialize against. For built-in types, the standard library already follows this convention — &lt;code&gt;Strings.t&lt;/code&gt; returns the string type, so you never need to write a bare type name directly.&lt;/p&gt;
&lt;p&gt;Now any record with the keys &lt;code&gt;name:&lt;/code&gt; and &lt;code&gt;age:&lt;/code&gt; responds to &lt;code&gt;birthday:&lt;/code&gt;:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
bob := { name: &#39;bob&#39;, age: 44 }
bob := bob.birthday
# :: Happy Birthday, bob!

bob.age  # :: 45
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Notice that &lt;code&gt;birthday&lt;/code&gt; returns a new record (with &lt;code&gt;age&lt;/code&gt; incremented) — it doesn&amp;rsquo;t mutate &lt;code&gt;bob&lt;/code&gt; in place. To &amp;ldquo;update&amp;rdquo; bob, you simply rebind the name.&lt;/p&gt;
&lt;h3&gt;Types&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;types&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#types&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;Types in Gab dictate how values respond to messages. This is a powerful tool for creating intuitive interfaces.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
Point   := { x: 0, y: 0 } ?

+: .def (Point, (dx, dy) :: do
    { x: (self.x + dx), y: (self.y + dy) }
end)

p := { x: 10 y: 10 }
p := p + (5, 15)
# :: { x: 15, y: 25 }
&lt;/code&gt;&lt;/pre&gt;

      </description>
    </item>
    
    <item>
      <title>Strings &amp; Binaries</title>
      <link>/docs/gabonomicon/basic_types/strings/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/gabonomicon/basic_types/strings/</guid>
      <description>
        
        
        &lt;p&gt;Gab has two string-like types: &lt;code&gt;gab\string&lt;/code&gt; and &lt;code&gt;gab\binary&lt;/code&gt;. A third type, &lt;code&gt;gab\message&lt;/code&gt;, also belongs to this family. All three &lt;strong&gt;share their character data in memory&lt;/strong&gt; — the bytes &lt;code&gt;[ &#39;t&#39;, &#39;r&#39;, &#39;u&#39;, &#39;e&#39; ]&lt;/code&gt; are stored once on the heap, shared between the string &lt;code&gt;&amp;quot;true&amp;quot;&lt;/code&gt; and the message &lt;code&gt;true:&lt;/code&gt;. Converting between these types is therefore a constant-time operation with no allocation.&lt;/p&gt;
&lt;h2&gt;&lt;code&gt;gab\string&lt;/code&gt;&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;gabstring&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#gabstring&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Strings are UTF-8 encoded byte sequences. Single-quoted strings support escape sequences; double-quoted strings do not.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
&#34;Hello!&#34;
&#39;\tHello\n&#39;
&#39;Hello \u[2502]&#39;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Because Gab respects UTF-8 encoding, operations that are trivial on raw bytes may be linear-time on strings. Slicing a string at a given index requires scanning from the start, since utf8 codepoints are 1–4 bytes wide.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
smiley := &#39;😀&#39;

smiley.len    # :: 1  (one codepoint)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Constructing strings:&lt;/strong&gt;&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
Strings.make(&#39;Ada&#39;, &#39; &#39;, last_name)

&#39;Format a value: $&#39;.sprintf({ name: &#39;bob&#39; })
# :: &#39;Format a value: { name: bob }&#39;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;sprintf&lt;/code&gt; replaces each &lt;code&gt;$&lt;/code&gt; in the format string with the corresponding argument, in order.&lt;/p&gt;
&lt;h2&gt;&lt;code&gt;gab\binary&lt;/code&gt;&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;gabbinary&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#gabbinary&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;&lt;code&gt;gab\binary&lt;/code&gt; operates on raw bytes with no encoding enforced. Indexing and slicing are constant-time because there are no multi-byte codepoints to account for.&lt;/p&gt;
&lt;p&gt;There is no literal syntax for binaries. Convert a string with &lt;code&gt;to\b&lt;/code&gt;:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
smiley     := &#39;😀&#39;
smiley_bin := smiley.to\b

smiley.len      # :: 1  (codepoints)
smiley_bin.len  # :: 4  (bytes)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Slicing a binary is constant-time:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
&#34;This is a string&#34;.slice(3, 8)        # Linear — scans from start
&#34;This is a string&#34;.to\b.slice(3, 8)   # Constant time — byte offset
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Converting a binary back to a string can fail if the bytes are not valid UTF-8:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
(status, str) := some_binary.as\s
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Shared memory with &lt;code&gt;gab\message&lt;/code&gt;&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;shared-memory-with-gabmessage&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#shared-memory-with-gabmessage&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Because &lt;code&gt;gab\string&lt;/code&gt;, &lt;code&gt;gab\binary&lt;/code&gt;, and &lt;code&gt;gab\message&lt;/code&gt; all share the same underlying character data, converting between them is zero-cost. The string &lt;code&gt;&#39;true&#39;&lt;/code&gt; and the message &lt;code&gt;true:&lt;/code&gt; occupy the same bytes — they differ only in their type tag.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
&#39;true&#39;.to\m   # :: true:   (no allocation)
true:.to\s    # :: &#39;true&#39;  (no allocation)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This design means message values are just as efficient as strings in any context where they appear as keys or identifiers.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title>Blocks</title>
      <link>/docs/gabonomicon/tour/blocks/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/gabonomicon/tour/blocks/</guid>
      <description>
        
        
        &lt;p&gt;A &lt;strong&gt;block&lt;/strong&gt; is Gab&amp;rsquo;s name for a closure - an anonymous function that can capture values from its surrounding scope. Blocks are values: you can store them in records, pass them as arguments to messages, and call them.&lt;/p&gt;
&lt;h2&gt;Block syntax&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;block-syntax&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#block-syntax&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The &lt;code&gt;::&lt;/code&gt; operator creates a block. The left-hand side is the &lt;strong&gt;binding&lt;/strong&gt; (the parameter list), and the right-hand side is the &lt;strong&gt;expression&lt;/strong&gt; that becomes the return value.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
double := (x) :: x * 2

double.(5)
# :: 10
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Blocks are called with an &lt;strong&gt;empty message send&lt;/strong&gt;. This is a bare &lt;code&gt;.&lt;/code&gt; followed by any arguments.&lt;/p&gt;
&lt;p&gt;For multi-line blocks, use &lt;code&gt;do ... end&lt;/code&gt; on the right-hand side. A &lt;code&gt;do ... end&lt;/code&gt; expression evaluates to the last expression before the &lt;code&gt;end&lt;/code&gt;:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
describe := (name, age) :: do
  line1 := &#39;Name: $!&#39;.sprintf(name)
  line2 := &#39;Age: $!&#39;.sprintf(age)
  Strings.make(line1 &#39;\n&#39; line2)
end

describe.(&#39;Alice&#39;, 30).println
# :: Name: Alice
# :: Age: 30
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Blocks with no parameters&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;blocks-with-no-parameters&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#blocks-with-no-parameters&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;If a block takes no arguments, use an empty tuple as the &lt;strong&gt;binding&lt;/strong&gt;. Call it with an empty message send, using &lt;code&gt;.&lt;/code&gt;.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
greet := () :: &#39;Hello!&#39;.println

greet.()
# :: Hello!
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Blocks as arguments&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;blocks-as-arguments&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#blocks-as-arguments&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Blocks are commonly passed as arguments to messages. You&amp;rsquo;ve already seen this with boolean branching:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
(temperature &gt; 80)
  .then(() :: &#39;Too hot!&#39;.println)
  .else(() :: &#39;Just right.&#39;.println)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Blocks are also useful as arguments to messages like &lt;code&gt;each&lt;/code&gt; and &lt;code&gt;map&lt;/code&gt;:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
[&#39;alice&#39;, &#39;bob&#39;, &#39;carol&#39;].each (name) :: do
  &#39;Hello, $!&#39;.sprintf(name).println
end

# :: Hello, alice
# :: Hello, bob
# :: Hello, carol
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Multiple return values&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;multiple-return-values&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#multiple-return-values&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Blocks can return more than one value using a &lt;strong&gt;tuple&lt;/strong&gt;.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
minmax := (a, b) :: do
  (a &lt; b)
    .then(() :: (a, b))
    .else(() :: (b, a))
end

(lo, hi) := minmax.(7, 3)

lo  # :: 3
hi  # :: 7
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;A note on commas&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;a-note-on-commas&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#a-note-on-commas&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;Commas are whitespace in Gab. Most of the time they are purely visual, to help us distiguish key-value pairs in a dictionary.
However, sometimes whitespace &lt;em&gt;does&lt;/em&gt; have syntactic meaning. Take a look at the below example.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
(1 + 2 3)   # :: (3, 3)  — `2` is the argument to `+`, `3` is the second element
(1 +, 2 3)  # :: Error   — the comma cuts off `+` before it gets an argument
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This doesn&amp;rsquo;t just happen with commas - gab treats commas, semi-colons, and new-lines all identically.&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;Multiple return values become especially important when combined with message chaining. When you chain a message send, &lt;strong&gt;all return values&lt;/strong&gt; from the left side are forwarded as the receiver and arguments of the next message. Given:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
IO.File.make(self).my_message
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This is exactly equivalent to:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
(status, file) := IO.File.make(self)
status.my_message(file)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The first return value becomes the receiver; subsequent return values become arguments. This is why the error-handling pattern in Gab is so fluid — &lt;code&gt;ok:&lt;/code&gt; and &lt;code&gt;err:&lt;/code&gt; can each respond differently to the same chained message, routing the remaining values accordingly. You&amp;rsquo;ll see this in detail in &lt;a href=&#34;/docs/tour/error_handling&#34;&gt;Error Handling&lt;/a&gt;.&lt;/p&gt;
&lt;h2&gt;Closures&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;closures&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#closures&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Blocks close over their surrounding scope. Any name visible where the block is defined is accessible inside it:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
prefix := &#39;Hello&#39;

greet := (name) :: do
  &#39;$!, $!&#39;.sprintf(prefix, name).println
end

greet.(&#39;world&#39;)
# :: Hello, world
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The block captures &lt;code&gt;prefix&lt;/code&gt; at the time it is defined. If you rebind &lt;code&gt;prefix&lt;/code&gt; later, the block still holds onto the original value — Gab&amp;rsquo;s immutability makes this safe and predictable.&lt;/p&gt;
&lt;h2&gt;Blocks and fibers&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;blocks-and-fibers&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#blocks-and-fibers&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;A block is the unit of work you hand to a fiber. When you spawn a fiber, you give it a block to execute. The block runs parallel in its own lightweight thread of execution. See &lt;a href=&#34;/docs/tour/fibers_and_channels&#34;&gt;Fibers &amp;amp; Channels&lt;/a&gt; for the full picture.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title>Messages</title>
      <link>/docs/gabonomicon/basic_types/messages/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/gabonomicon/basic_types/messages/</guid>
      <description>
        
        
        &lt;p&gt;A message is a unique value whose type is itself.&lt;/p&gt;
&lt;h2&gt;&lt;code&gt;gab\message&lt;/code&gt;&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;gabmessage&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#gabmessage&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Messages values are a name or operator ending with a &lt;code&gt;:&lt;/code&gt;.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
+:
true:
&lt;=!=&gt;:

ok:?      # :: ok:
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Messages are used as record keys, as sentinel/enum values, and as the mechanism for polymorphism. They are Gab&amp;rsquo;s implementation of booleans, nil, and result values — there are no built-in keywords for any of these.&lt;/p&gt;
&lt;h2&gt;Defining specializations&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;defining-specializations&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#defining-specializations&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Messages responds to &lt;code&gt;def:&lt;/code&gt;. This adds a new &lt;strong&gt;specialization&lt;/strong&gt; of that message for a specific receiver type:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
greet: .def (Strings.t, () :: do
  &#39;Hello, $!&#39;.sprintf(self).println
end)

&#39;Alice&#39;.greet   # :: Hello, Alice!
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;There are multiple ways to define messages, and it is commonplace to create your own.&lt;/p&gt;
&lt;h3&gt;&lt;code&gt;def:&lt;/code&gt;&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;def&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#def&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;Defines a single specialization for a single type:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
birthday: .def (Person, () :: do
  self.put(age: self.age + 1)
end)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;If you omit the type, you create a &lt;em&gt;general&lt;/em&gt; specialization. Think of this as a fallback implementation which will run if nothing more specific exists.&lt;/p&gt;
&lt;h3&gt;&lt;code&gt;defcase:&lt;/code&gt;&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;defcase&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#defcase&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;Defines multiple specializations for one message at once, using a record. Each key-value pair in the record is used to create a new type-specialization.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
describe: .defcase {
  ok:   result :: &#39;Success: $&#39;.sprintf(result).println
  err:  msg    :: &#39;Error: $&#39;.sprintf(msg).println
  nil:          :: &#39;Nothing here.&#39;.println
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Each key in the record is a receiver type; each value is the block to call when that type receives the message. Values alone (without a block) are also valid — they are returned directly.&lt;/p&gt;
&lt;h3&gt;&lt;code&gt;defmodule:&lt;/code&gt;&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;defmodule&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#defmodule&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;Defines multiple messages for multiple receiver types at once:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
[Point, Vector] .defmodule {
  scale: (factor) :: self.put(x: self.x * factor, y: self.y * factor)
  zero:  ()       :: self.put(x: 0, y: 0)
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Dispatch resolution order&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;dispatch-resolution-order&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#dispatch-resolution-order&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;When a message is sent to a value, Gab resolves the specialization in this order:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Super type&lt;/strong&gt; — if the value&amp;rsquo;s type (e.g. a &lt;code&gt;gab\shape&lt;/code&gt;) has a specialization, use it.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Type&lt;/strong&gt; — use the specialization defined for the value&amp;rsquo;s &lt;code&gt;gab\&lt;/code&gt; type (e.g. &lt;code&gt;gab\record&lt;/code&gt;).&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Property&lt;/strong&gt; — if the receiver is a record and the message name matches one of its keys, return that value. This is how field access works: &lt;code&gt;{ name: &#39;bob&#39; }.name&lt;/code&gt; returns &lt;code&gt;&#39;bob&#39;&lt;/code&gt; without any explicit &lt;code&gt;def:&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;General&lt;/strong&gt; — use a specialization defined with no specific type.&lt;/li&gt;
&lt;/ol&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
y: .def &#39;general case&#39;

z: .def (Shapes.make(x:), &#39;shape case&#39;)

{ x: 1 }.y   # :: &#39;general case&#39;  (general)
{ x: 1 }.z   # :: &#39;shape case&#39;    (super type — the shape &lt;x:&gt;)
{ x: 1 }.x   # :: 1               (property)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;&lt;code&gt;and:&lt;/code&gt; &lt;code&gt;or:&lt;/code&gt; &lt;code&gt;then:&lt;/code&gt; &lt;code&gt;else:&lt;/code&gt;&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;and-or-then-else&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#and-or-then-else&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;These messages are defined on &lt;code&gt;true:&lt;/code&gt; and &lt;code&gt;false:&lt;/code&gt; in the core library. Their semantics differ in one important way:&lt;/p&gt;
&lt;p&gt;&lt;code&gt;and:&lt;/code&gt; and &lt;code&gt;or:&lt;/code&gt; accept &lt;strong&gt;values&lt;/strong&gt;. The argument is always evaluated before the message is sent:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
true:  .and 2    # :: 2
false: .and 2    # :: false:
false: .or  2    # :: 2
true:  .or  2    # :: true:
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;then:&lt;/code&gt; and &lt;code&gt;else:&lt;/code&gt; accept &lt;strong&gt;blocks&lt;/strong&gt;. Only the appropriate branch is invoked:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
true: .then  () :: &#39;yes&#39;.println   # :: yes
true: .else  () :: &#39;no&#39;.println    # (block is never called)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;&lt;code&gt;nil:&lt;/code&gt; &lt;code&gt;none:&lt;/code&gt;&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;nil-none&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#nil-none&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;&lt;code&gt;nil:&lt;/code&gt; is the value Gab binds to names that have no corresponding value. This may occur if, for example,  a binding list is longer than the tuple being destructured:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
(a, b) := 1   # a :: 1, b :: nil:
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;none:&lt;/code&gt; is used by certain APIs to signal the absence of a result (as opposed to an error). Both are &lt;code&gt;nil:&lt;/code&gt; and &lt;code&gt;none:&lt;/code&gt; are just plain messages.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title>Pub/Sub Broker</title>
      <link>/docs/gabonomicon/examples/pubsub/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/gabonomicon/examples/pubsub/</guid>
      <description>
        
        
        &lt;p&gt;This example builds a publish/subscribe message broker. Publishers send events to a topic; all subscribers to that topic receive them. The broker is a single actor fiber — the same pattern as the key-value store, with more interesting state.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;The Design&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;the-design&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#the-design&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The broker owns a record mapping topic names to subscriber lists. Each subscriber list is a record used as a growable list of channels.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
state := {
  news:  [ chan_a, chan_b ],
  sport: [ chan_c ]
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Three commands:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;broker\subscribe:&lt;/code&gt;&lt;/strong&gt; — add a channel to a topic&amp;rsquo;s subscriber list&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;broker\unsubscribe:&lt;/code&gt;&lt;/strong&gt; — remove a channel from a topic&amp;rsquo;s subscriber list&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;broker\publish:&lt;/code&gt;&lt;/strong&gt; — send an event to every subscriber on a topic&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Fan-out spawns one fiber per subscriber, so a slow subscriber never stalls the broker or other subscribers.&lt;/p&gt;
&lt;h2&gt;The Broker Actor&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;the-broker-actor&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#the-broker-actor&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The broker channel carries &lt;code&gt;(cmd, args*)&lt;/code&gt; tuples. No reply channel is needed since callers don&amp;rsquo;t wait for a result from any of the three commands.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
Broker := broker:

make: .def (Broker, () :: do
  ch := Channels.make

  loop := (state) :: do
    (cmd, args*) := ch &gt;! .unwrap
    next_state := (cmd, state, args*) .handle
    self.(next_state)
  end

  Fibers.make () :: loop.({})

  ch
end)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;The Command Handlers&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;the-command-handlers&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#the-command-handlers&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;&lt;strong&gt;&lt;code&gt;broker\subscribe:&lt;/code&gt;&lt;/strong&gt; uses &lt;code&gt;put_via_by:&lt;/code&gt; to append the new channel to the topic&amp;rsquo;s subscriber list with &lt;code&gt;cons:&lt;/code&gt;. If the topic doesn&amp;rsquo;t exist yet, &lt;code&gt;put_via_by:&lt;/code&gt; initialises it automatically.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;&lt;code&gt;broker\unsubscribe:&lt;/code&gt;&lt;/strong&gt; checks the topic exists with &lt;code&gt;at:&lt;/code&gt;, then uses &lt;code&gt;put_by:&lt;/code&gt; with a transformation block to rebuild the subscriber list in place. &lt;code&gt;filter:&lt;/code&gt; keeps every channel where &lt;code&gt;c == ch !&lt;/code&gt; is true (the &lt;code&gt;!&lt;/code&gt; negates, so channels that are &lt;em&gt;not&lt;/em&gt; &lt;code&gt;ch&lt;/code&gt; are kept). &lt;code&gt;.or(state)&lt;/code&gt; returns &lt;code&gt;state&lt;/code&gt; unchanged if the topic doesn&amp;rsquo;t exist.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;&lt;code&gt;broker\publish:&lt;/code&gt;&lt;/strong&gt; iterates over subscribers and spawns a fiber per delivery. &lt;code&gt;state&lt;/code&gt; is returned unconditionally after the &lt;code&gt;then:&lt;/code&gt; chain.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
handle: .defcase {
  broker\subscribe: (state, topic, ch) :: do
    state.put_via_by(topic subs :: subs.cons ch)
  end

  broker\unsubscribe: (state, topic, ch) :: do
    state.at(topic)
      .then(() :: do
        after := state.put_by(topic, subs :: subs.filter(c :: c == ch !))
        after
      end)
      .or(state)
  end

  broker\publish: (state, topic, event) :: do
    state.at(topic)
      .then(subs :: do
        subs.each sub :: do
          Fibers.make () :: sub &lt;! event
        end
      end)

    state
  end
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;The Public API&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;the-public-api&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#the-public-api&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;All three methods are fire-and-forget. &lt;code&gt;broker\unsubscribe:&lt;/code&gt; takes both the topic and the channel. The topic is needed to locate the subscriber list.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
t: .def (Broker, Channels.t)

[Broker.t] .defmodule {
  broker\subscribe: (topic, ch) :: do
    self &lt;! (broker\subscribe: topic ch)
  end

  broker\unsubscribe: (topic, ch) :: do
    self &lt;! (broker\unsubscribe: topic ch)
  end

  broker\publish: (topic, event) :: do
    self &lt;! (broker\publish: topic event)
  end
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Putting it Together&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;putting-it-together&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#putting-it-together&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Because the broker processes commands asynchronously, subscribers must confirm they have registered before the publisher starts sending, and the publisher must wait for unsubscription to complete before sending to a topic the subscriber has left. A shared &lt;code&gt;done&lt;/code&gt; channel coordinates all of this:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
broker := Broker.make
done   := Channels.make

Fibers.make () :: do
  inbox := Channels.make
  broker.broker\subscribe(news: inbox)

  done &lt;!         # signal: subscribed

  inbox.each (event) :: do
    &#39;NEWS: $&#39;.sprintf(event).println
  end
end

done &gt;!           # wait for news subscriber

Fibers.make () :: do
  inbox := Channels.make
  broker.broker\subscribe(sport: inbox)

  done &lt;!         # signal: subscribed

  inbox.each (event) :: do
    &#39;SPORT: $&#39;.sprintf(event).println

    broker.broker\unsubscribe(sport: inbox)
    done &lt;!       # signal: unsubscribed
  end
end

done &gt;!           # wait for sport subscriber

broker.broker\publish(news:  &#39;Gab 1.0 released&#39;)
broker.broker\publish(sport: &#39;Final score: 3-1&#39;)
broker.broker\publish(news:  &#39;Another story&#39;)

done &gt;!           # wait for sport subscriber to unsubscribe

# This event has no subscribers — the sport listener
# unsubscribed itself after receiving the first event.
broker.broker\publish(sport: &#39;Final score: 6-7&#39;)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The sport subscriber unsubscribes after receiving its first event, then signals &lt;code&gt;done&lt;/code&gt; to tell the main fiber it is safe to send the second sport event. The final publish is silently dropped — no subscribers remain on the &lt;code&gt;sport:&lt;/code&gt; topic.&lt;/p&gt;
&lt;h2&gt;The Full Program&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;the-full-program&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#the-full-program&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The full broker module is below.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
Broker := broker:

make: .def (Broker, () :: do
  ch := Channels.make

  loop := (state) :: do
    (cmd, args*) := ch &gt;! .unwrap
    next_state := (cmd, state, args*) .handle
    self.(next_state)
  end

  Fibers.make () :: loop.({})

  ch
end)

handle: .defcase {
  broker\subscribe: (state, topic, ch) :: do
    state.put_via_by(topic subs :: subs.cons ch)
  end

  broker\unsubscribe: (state, topic, ch) :: do
    state.at(topic)
      .then(() :: do
        after := state.put_by(topic, subs :: subs.filter(c :: c == ch !))
        after
      end)
      .or(state)
  end

  broker\publish: (state, topic, event) :: do
    state.at(topic)
      .then(subs :: do
        subs.each sub :: do
          Fibers.make () :: ok := sub &lt;! event
        end
      end)

    state
  end
}

t: .def (Broker, Channels.t)

[Broker.t] .defmodule {
  broker\subscribe: (topic, ch) :: do
    self &lt;! (broker\subscribe: topic ch)
  end

  broker\unsubscribe: (topic, ch) :: do
    self &lt;! (broker\unsubscribe: topic ch)
  end

  broker\publish: (topic, event) :: do
    self &lt;! (broker\publish: topic event)
  end
}

Broker
&lt;/code&gt;&lt;/pre&gt;

      </description>
    </item>
    
    <item>
      <title>Records</title>
      <link>/docs/gabonomicon/basic_types/records/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/gabonomicon/basic_types/records/</guid>
      <description>
        
        
        &lt;p&gt;Records are the only data structure in Gab. They serve as both dictionareis and lists.&lt;/p&gt;
&lt;h2&gt;&lt;code&gt;gab\record&lt;/code&gt;&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;gabrecord&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#gabrecord&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;h3&gt;Dictionaries&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;dictionaries&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#dictionaries&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;Dictionary-style records allow arbitrary keys. They are constructed with &lt;code&gt;{}&lt;/code&gt;, with keys and values in pairs:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
a_record := { name: &#39;bob&#39;, age: 44 }
another  := { key: &#39;value&#39;, &#39;another_key&#39; 10 }
&lt;/code&gt;&lt;/pre&gt;
&lt;div class=&#34;hx:overflow-x-auto hx:mt-6 hx:flex hx:flex-col hx:rounded-lg hx:border hx:py-4 hx:px-4 hx:border-gray-200 hx:contrast-more:border-current hx:contrast-more:dark:border-current hx:border-blue-200 hx:bg-blue-100 hx:text-blue-900 hx:dark:border-blue-200/30 hx:dark:bg-blue-900/30 hx:dark:text-blue-200&#34;&gt;
  &lt;p class=&#34;hx:flex hx:items-center hx:font-medium&#34;&gt;&lt;svg height=16px class=&#34;hx:inline-block hx:align-middle hx:mr-2&#34; xmlns=&#34;http://www.w3.org/2000/svg&#34; fill=&#34;none&#34; viewBox=&#34;0 0 24 24&#34; stroke-width=&#34;2&#34; stroke=&#34;currentColor&#34; aria-hidden=&#34;true&#34;&gt;&lt;path stroke-linecap=&#34;round&#34; stroke-linejoin=&#34;round&#34; d=&#34;M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z&#34;/&gt;&lt;/svg&gt;Note&lt;/p&gt;

  &lt;div class=&#34;hx:w-full hx:min-w-0 hx:leading-7&#34;&gt;
    &lt;div class=&#34;hx:mt-6 hx:leading-7 hx:first:mt-0&#34;&gt;&lt;p&gt;Note that keys can be any value. Messages and strings are the most common, but numbers, blocks, channels, even other records are all valid keys.&lt;/p&gt;&lt;/div&gt;
  &lt;/div&gt;
&lt;/div&gt;
&lt;h3&gt;Lists&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;lists&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#lists&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;List-style records use increasing integer keys starting from zero. They are constructed with &lt;code&gt;[]&lt;/code&gt;:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
a_list := [1 2 3]
a_list   # :: [1, 2, 3]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;A list can also be constructed using explicit integer keys in dictionary syntax. Gab recognises the shape and still displays it as a list:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
a_list := { 0 1, 1 2, 2 3 }
a_list   # :: [1, 2, 3]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The &lt;code&gt;is\list:&lt;/code&gt; message is used to determine if a given record is a list.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
[1 2].is\list # :: true:
{ name: &#39;Rich&#39; }.is\list # :: false:
&lt;/code&gt;&lt;/pre&gt;
&lt;div class=&#34;hx:overflow-x-auto hx:mt-6 hx:flex hx:flex-col hx:rounded-lg hx:border hx:py-4 hx:px-4 hx:border-gray-200 hx:contrast-more:border-current hx:contrast-more:dark:border-current hx:border-blue-200 hx:bg-blue-100 hx:text-blue-900 hx:dark:border-blue-200/30 hx:dark:bg-blue-900/30 hx:dark:text-blue-200&#34;&gt;
  &lt;p class=&#34;hx:flex hx:items-center hx:font-medium&#34;&gt;&lt;svg height=16px class=&#34;hx:inline-block hx:align-middle hx:mr-2&#34; xmlns=&#34;http://www.w3.org/2000/svg&#34; fill=&#34;none&#34; viewBox=&#34;0 0 24 24&#34; stroke-width=&#34;2&#34; stroke=&#34;currentColor&#34; aria-hidden=&#34;true&#34;&gt;&lt;path stroke-linecap=&#34;round&#34; stroke-linejoin=&#34;round&#34; d=&#34;M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z&#34;/&gt;&lt;/svg&gt;Note&lt;/p&gt;

  &lt;div class=&#34;hx:w-full hx:min-w-0 hx:leading-7&#34;&gt;
    &lt;div class=&#34;hx:mt-6 hx:leading-7 hx:first:mt-0&#34;&gt;&lt;p&gt;The empty record &lt;code&gt;{}&lt;/code&gt; is a list, whether you write it like &lt;code&gt;[]&lt;/code&gt; or like &lt;code&gt;{}&lt;/code&gt;.&lt;/p&gt;&lt;/div&gt;
  &lt;/div&gt;
&lt;/div&gt;
&lt;h2&gt;List-to-dictionary transitions&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;list-to-dictionary-transitions&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#list-to-dictionary-transitions&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Adding a non-integer key to a list produces a dictionary:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
a_list := [1 2 3]
a_list := a_list.put(name: &#39;bob&#39;)
# :: { 0: 1, 1: 2, 2: 3, name: &#39;bob&#39; }
&lt;/code&gt;&lt;/pre&gt;

      </description>
    </item>
    
    <item>
      <title>Fibers &amp; Channels</title>
      <link>/docs/gabonomicon/tour/fibers_and_channels/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/gabonomicon/tour/fibers_and_channels/</guid>
      <description>
        
        
        &lt;p&gt;Parallelism is not an add-on in Gab. It is a first-class feature of the language and runtime. Two foundational primitives are provided: &lt;strong&gt;fibers&lt;/strong&gt; and &lt;strong&gt;channels&lt;/strong&gt;.&lt;/p&gt;
&lt;h2&gt;Fibers&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;fibers&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#fibers&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;A fiber is a lightweight unit of execution, similar to a goroutine in Go or a process on the BEAM (Erlang/Elixir). Fibers are cheap: Gab&amp;rsquo;s runtime is designed to support hundreds of thousands of them running concurrently.&lt;/p&gt;
&lt;p&gt;You spawn a fiber by passing a block to &lt;code&gt;Fibers.make&lt;/code&gt;:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
Fibers.make () :: do
  &#39;Hello from a fiber!&#39;.println
end
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The block runs concurrently. The fiber is scheduled by Gab&amp;rsquo;s runtime — you don&amp;rsquo;t manage threads or thread pools.&lt;/p&gt;
&lt;p&gt;Here&amp;rsquo;s a more complete example that spawns 20,000 fibers:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
spawn_task := (i) :: do
  Fibers.make () :: do
    &#39;Hello from fiber $!&#39;.sprintf(i).println
  end
end

Ranges.make(0, 20000).each spawn_task
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Channels&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;channels&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#channels&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Fibers communicate with each other through &lt;strong&gt;channels&lt;/strong&gt;. A channel is the &lt;em&gt;only&lt;/em&gt; way for two fibers to exchange data, or synchronize.&lt;/p&gt;
&lt;p&gt;Create a channel with &lt;code&gt;Channels.make&lt;/code&gt;:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
ch := Channels.make
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Send&lt;/strong&gt; a value into a channel with the &lt;code&gt;&amp;lt;!&lt;/code&gt; operator:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
ch &lt;! &#39;a message&#39;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Receive&lt;/strong&gt; a value from a channel with the &lt;code&gt;&amp;gt;!&lt;/code&gt; operator:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
value := ch &gt;!
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Gab&amp;rsquo;s channels are &lt;strong&gt;unbuffered&lt;/strong&gt;: a send blocks until a receiver is ready, and a receive blocks until a sender is ready.
This keeps communication explicit and synchronised.&lt;/p&gt;
&lt;div class=&#34;hx:overflow-x-auto hx:mt-6 hx:flex hx:flex-col hx:rounded-lg hx:border hx:py-4 hx:px-4 hx:border-gray-200 hx:contrast-more:border-current hx:contrast-more:dark:border-current hx:border-blue-200 hx:bg-blue-100 hx:text-blue-900 hx:dark:border-blue-200/30 hx:dark:bg-blue-900/30 hx:dark:text-blue-200&#34;&gt;
  &lt;p class=&#34;hx:flex hx:items-center hx:font-medium&#34;&gt;&lt;svg height=16px class=&#34;hx:inline-block hx:align-middle hx:mr-2&#34; xmlns=&#34;http://www.w3.org/2000/svg&#34; fill=&#34;none&#34; viewBox=&#34;0 0 24 24&#34; stroke-width=&#34;2&#34; stroke=&#34;currentColor&#34; aria-hidden=&#34;true&#34;&gt;&lt;path stroke-linecap=&#34;round&#34; stroke-linejoin=&#34;round&#34; d=&#34;M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z&#34;/&gt;&lt;/svg&gt;Note&lt;/p&gt;

  &lt;div class=&#34;hx:w-full hx:min-w-0 hx:leading-7&#34;&gt;
    &lt;div class=&#34;hx:mt-6 hx:leading-7 hx:first:mt-0&#34;&gt;&lt;p&gt;Channels actually accept &lt;em&gt;tuples&lt;/em&gt; instead of just single values. You can send as many values as you want, and they will be transferred as a group.&lt;/p&gt;&lt;/div&gt;
  &lt;/div&gt;
&lt;/div&gt;
&lt;h2&gt;Putting it together&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;putting-it-together&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#putting-it-together&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Here is a pipeline where many fibers produce values, and a single consumer reads them all:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
print_chan := Channels.make

Ranges.make(0, 10000).each i :: do
  Fibers.make () :: do
    print_chan &lt;! &#39;Hello from fiber $!&#39;.sprintf(i)
  end
end

print_chan.each (msg) :: msg.println
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Each fiber sends one message into the channel, then exits. The &lt;code&gt;each&lt;/code&gt; message reads values from the channel and passes each one to the block.&lt;/p&gt;
&lt;h2&gt;Zero-copy message passing&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;zero-copy-message-passing&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#zero-copy-message-passing&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;A common cost in concurrent systems is &lt;strong&gt;copying&lt;/strong&gt;: when you send data to another thread, the runtime must copy it to keep both sides safe. Gab eliminates this cost.&lt;/p&gt;
&lt;p&gt;Because all of Gab&amp;rsquo;s data structures are immutable, a value cannot change after it is created. This means it is always safe to share a reference to a value across fiber boundaries — no copying is needed. In practice, passing a large record between 10,000 fibers is no more expensive than passing an integer.&lt;/p&gt;
&lt;p&gt;This is a deliberate design choice that makes Gab&amp;rsquo;s concurrency both safe and fast.&lt;/p&gt;
&lt;h2&gt;Channels are immutable too&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;channels-are-immutable-too&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#channels-are-immutable-too&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Even &lt;code&gt;gab\channel&lt;/code&gt; is immutable. A channel reference can be passed freely between fibers without any synchronisation overhead. The runtime handles the scheduling of sends and receives internally.&lt;/p&gt;
&lt;h2&gt;Atoms: safe shared state&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;atoms-safe-shared-state&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#atoms-safe-shared-state&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Channels and fibers are sufficient to implement any concurrency abstraction you might need.
Here is a sample implementation of an &lt;strong&gt;atom&lt;/strong&gt;. Think of this as a value that can be read and updated safely from any fiber, similar to Clojure&amp;rsquo;s &lt;code&gt;atom&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;The design: a dedicated fiber holds the current state privately in its own local scope.
Other fibers send commands to it over a channel, where each command is a tuple of a reply channel and a function to apply.
The atom fiber applies the function, sends the new state back on the reply channel, and recurses with the updated state.
Because all reads and writes go through a single fiber, no two updates can race.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
Atom := gab\atom:

make: .def (Atom, (initial) :: do
  ch := Channels.make

  loop := (state) :: do
    (reply, f) := ch &gt;!
    new_state   := f.(state)
    reply &lt;! new_state
    self.(new_state)
  end

  Fibers.make () :: loop.(initial)

  { chan: ch }
end)

t: .def (Atom, () :: { chan: nil: }?)

[Atom.t] .defmodule {
  deref: () :: do
    reply := Channels.make
    self.chan &lt;! (reply, (x) :: x)
    reply &gt;!
  end

  swap: (f) :: do
    reply := Channels.make
    self.chan &lt;! (reply, f)
    reply &gt;!
  end

  reset: (val) :: self.swap(() :: val)
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Usage:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
counter := Atom.make(0)

counter.deref         # :: 0

counter.swap((n) :: n + 1)
counter.swap((n) :: n + 1)

counter.deref         # :: 2

counter.reset(100)
counter.deref         # :: 100
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;A few things worth noting in the implementation:&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Recursive blocks via &lt;code&gt;self&lt;/code&gt;.&lt;/strong&gt; &lt;code&gt;loop&lt;/code&gt; calls &lt;code&gt;self.(new_state)&lt;/code&gt; to recurse. When a block is invoked directly rather than as a message specialization, &lt;code&gt;self&lt;/code&gt; refers to the block itself — making this a clean tail-recursive loop with no stack growth.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Tuples over channels.&lt;/strong&gt; &lt;code&gt;ch &amp;lt;! (reply, f)&lt;/code&gt; sends both the reply channel and the function as a single tuple. The atom fiber receives and destructures them in one step with &lt;code&gt;(reply, f) := ch &amp;gt;!&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;&lt;code&gt;t:&lt;/code&gt; provides the shape for &lt;code&gt;defmodule&lt;/code&gt;.&lt;/strong&gt; &lt;code&gt;Atom.t&lt;/code&gt; returns the shape of atom records — &lt;code&gt;&amp;lt;gab\shape chan:&amp;gt;&lt;/code&gt; — so that &lt;code&gt;defmodule&lt;/code&gt; has a concrete type to attach messages to. &lt;code&gt;self.chan&lt;/code&gt; inside the module accesses the channel field via property dispatch.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;&lt;code&gt;deref&lt;/code&gt; is just &lt;code&gt;swap&lt;/code&gt; with the identity function.&lt;/strong&gt; There&amp;rsquo;s no separate read mechanism — the same serialised path handles both reads and writes, which guarantees that a &lt;code&gt;deref&lt;/code&gt; sees all preceding &lt;code&gt;swap!&lt;/code&gt; calls.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title>Error Handling</title>
      <link>/docs/gabonomicon/tour/error_handling/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/gabonomicon/tour/error_handling/</guid>
      <description>
        
        
        &lt;p&gt;Gab does not have exceptions. There is no &lt;code&gt;try/catch&lt;/code&gt;, no &lt;code&gt;throw&lt;/code&gt;, and no stack unwinding.
Instead, operations that can fail &lt;strong&gt;return their errors as values&lt;/strong&gt;.
You handle errors the same way you do anything else: by sending messages.&lt;/p&gt;
&lt;h2&gt;Errors as return values&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;errors-as-return-values&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#errors-as-return-values&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;A message that can fail returns multiple values. The first is a status, typically either &lt;code&gt;ok:&lt;/code&gt; or &lt;code&gt;err:&lt;/code&gt;. The second is either the result or an explanation of what went wrong.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
(status, file) := IO.File.make(&#39;my_file.txt&#39;)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;If the file is opened successfully, &lt;code&gt;status&lt;/code&gt; is &lt;code&gt;ok:&lt;/code&gt; and &lt;code&gt;file&lt;/code&gt; is a &lt;code&gt;gab\box IO.File&lt;/code&gt; you can read from or write to.&lt;/p&gt;
&lt;p&gt;If something went wrong: maybe the file doesn&amp;rsquo;t exist, or permissions are denied, then &lt;code&gt;status&lt;/code&gt; is &lt;code&gt;err:&lt;/code&gt; and &lt;code&gt;file&lt;/code&gt; is a string describing the error.&lt;/p&gt;
&lt;h2&gt;Handling the result&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;handling-the-result&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#handling-the-result&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Because &lt;code&gt;ok:&lt;/code&gt; and &lt;code&gt;err:&lt;/code&gt; are just values, you handle them with messages. The verbose approach binds each return value and branches explicitly:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
(status, file) := IO.File.make(&#39;my_file.txt&#39;)

status
  .then(() :: file.read.println)
  .else(() :: &#39;Failed to open file: $!&#39;.sprintf(file).println)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;But recall how message chaining works with multiple return values: when you chain a message after a call that returns multiple values, the first return value becomes the receiver and the rest become arguments.
This means you can write the same thing as a single chain:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
IO.File.make(&#39;my_file.txt&#39;)
  .then((file) :: file.read.println)
  .else((msg)  :: &#39;Failed to open file: $!&#39;.sprintf(msg).println)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;ok:&lt;/code&gt; and &lt;code&gt;err:&lt;/code&gt; respond differently to &lt;code&gt;then:&lt;/code&gt; and &lt;code&gt;else:&lt;/code&gt;. &lt;code&gt;ok:&lt;/code&gt; calls its &lt;code&gt;then:&lt;/code&gt; block and passes the file through; &lt;code&gt;err:&lt;/code&gt; calls its &lt;code&gt;else:&lt;/code&gt; block and passes the error message through. The branching is built into the types, not into special syntax.&lt;/p&gt;
&lt;h2&gt;Why not exceptions?&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;why-not-exceptions&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#why-not-exceptions&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Exceptions make control flow invisible. An exception thrown inside a deeply nested call can unwind the entire stack, landing in a &lt;code&gt;catch&lt;/code&gt; block far removed from where the problem occurred. This makes programs harder to reason about, and harder to write reliable concurrent code with.&lt;/p&gt;
&lt;p&gt;Returning errors as values keeps the failure path explicit. When a message can fail, its type signature says so. You can&amp;rsquo;t accidentally ignore the error and proceed as if everything succeeded — the status value is right there in the binding.&lt;/p&gt;
&lt;p&gt;This approach is familiar if you&amp;rsquo;ve used Go, Rust&amp;rsquo;s &lt;code&gt;Result&lt;/code&gt; type, or Erlang&amp;rsquo;s &lt;code&gt;{:ok, value} | {:error, reason}&lt;/code&gt; convention. Gab follows the same discipline.&lt;/p&gt;
&lt;h2&gt;Propagating errors&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;propagating-errors&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#propagating-errors&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;If you want to pass an error up to the caller, return a tuple:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
read_config: .def (Strings.t, () :: do
  IO.File.make(self)
    .then((file) :: (ok: file.read))
    .else((msg)  :: (err: &#39;Could not open config: $!&#39;.sprintf(msg)))
end)
&lt;/code&gt;&lt;/pre&gt;

      </description>
    </item>
    
    <item>
      <title>Shapes</title>
      <link>/docs/gabonomicon/basic_types/shapes/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/gabonomicon/basic_types/shapes/</guid>
      <description>
        
        
        &lt;p&gt;A shape describes the keys of a record, in order. It is the record&amp;rsquo;s type. All records with the same keys in the same order share exactly one shape value.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
a := { name: &#39;Joe&#39; }
b := { name: &#39;Rich&#39; }

a?          # :: &lt;gab\shape name:&gt;
b?          # :: &lt;gab\shape name:&gt;
(a?) == (b?)  # :: true:
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Obtaining a shape&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;obtaining-a-shape&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#obtaining-a-shape&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;There are multiple ways to get a shape value.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;From a record&lt;/strong&gt;, using the &lt;code&gt;?&lt;/code&gt; message:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
Person := { name: &#39;&#39;, age: 0 }?
# :: &lt;gab\shape name: age:&gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Directly&lt;/strong&gt;, using &lt;code&gt;Shapes.make:&lt;/code&gt; with a list of keys, or the shape syntax:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
Person := Shapes.make(name:, age:)
# :: &lt;gab\shape name: age:&gt;
Person := \{ name: age: }
# :: &lt;gab\shape name: age:&gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Both produce the same shape value. The &lt;code&gt;?&lt;/code&gt; approach is convenient when you already have an example record; &lt;code&gt;Shapes.make&lt;/code&gt; is useful when you want to define the shape without constructing a record first.&lt;/p&gt;
&lt;h2&gt;Shapes as specialization targets&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;shapes-as-specialization-targets&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#shapes-as-specialization-targets&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Shapes are most useful as receiver types in &lt;code&gt;def:&lt;/code&gt;, &lt;code&gt;defcase:&lt;/code&gt;, and &lt;code&gt;defmodule:&lt;/code&gt;. All records with a matching shape will respond to the defined message:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
Person := Shapes.make(name:, age:)

birthday: .def (Person, () :: do
  &#39;Happy Birthday, $!&#39;.sprintf(self.name).println
  self.put(age: self.age + 1)
end)

bob := { name: &#39;bob&#39;, age: 44 }
bob := bob.birthday
# :: Happy Birthday, bob!

bob.age   # :: 45
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Shapes in dispatch&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;shapes-in-dispatch&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#shapes-in-dispatch&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;When Gab resolves a message send, the shape is checked as the &lt;strong&gt;super type&lt;/strong&gt; before the record&amp;rsquo;s base type (&lt;code&gt;gab\record&lt;/code&gt;). This means shape-specialised messages take precedence over general record behaviour:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
z: .def (\{ x: }, &#39;shape case&#39;)
z: .def (Records.t, &#39;record case&#39;)
z: .def &#39;general case&#39;

{ x: 1 }.z          # :: &#39;shape case&#39;   (shape wins over general)
{ y: 1 }.z          # :: &#39;record case&#39;
&#39;something else!&#39;.z # :: &#39;general case&#39;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;See &lt;a href=&#34;/docs/basic_types/messages#dispatch-resolution-order&#34;&gt;Messages — Dispatch Resolution Order&lt;/a&gt; for the full sequence.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title>Blocks</title>
      <link>/docs/gabonomicon/basic_types/blocks/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/gabonomicon/basic_types/blocks/</guid>
      <description>
        
        
        &lt;p&gt;A block is a function that captures its surrounding scope. Blocks are values: they can be stored, passed as arguments, and respond to messages.&lt;/p&gt;
&lt;h2&gt;&lt;code&gt;gab\block&lt;/code&gt;&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;gabblock&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#gabblock&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
square := (x) :: x * x
square.(4)   # :: 16
&lt;/code&gt;&lt;/pre&gt;
&lt;div class=&#34;hx:overflow-x-auto hx:mt-6 hx:flex hx:flex-col hx:rounded-lg hx:border hx:py-4 hx:px-4 hx:border-gray-200 hx:contrast-more:border-current hx:contrast-more:dark:border-current hx:border-blue-200 hx:bg-blue-100 hx:text-blue-900 hx:dark:border-blue-200/30 hx:dark:bg-blue-900/30 hx:dark:text-blue-200&#34;&gt;
  &lt;p class=&#34;hx:flex hx:items-center hx:font-medium&#34;&gt;&lt;svg height=16px class=&#34;hx:inline-block hx:align-middle hx:mr-2&#34; xmlns=&#34;http://www.w3.org/2000/svg&#34; fill=&#34;none&#34; viewBox=&#34;0 0 24 24&#34; stroke-width=&#34;2&#34; stroke=&#34;currentColor&#34; aria-hidden=&#34;true&#34;&gt;&lt;path stroke-linecap=&#34;round&#34; stroke-linejoin=&#34;round&#34; d=&#34;M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z&#34;/&gt;&lt;/svg&gt;Note&lt;/p&gt;

  &lt;div class=&#34;hx:w-full hx:min-w-0 hx:leading-7&#34;&gt;
    &lt;div class=&#34;hx:mt-6 hx:leading-7 hx:first:mt-0&#34;&gt;&lt;p&gt;Every block has an implicit &lt;code&gt;self&lt;/code&gt; binding. When a block is used as a message specialization via &lt;code&gt;def:&lt;/code&gt;, &lt;code&gt;self&lt;/code&gt; refers to the value that received the message. When called directly, &lt;code&gt;self&lt;/code&gt; refers to the block itself.&lt;/p&gt;&lt;/div&gt;
  &lt;/div&gt;
&lt;/div&gt;
&lt;h2&gt;Variadic bindings&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;variadic-bindings&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#variadic-bindings&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;There are two kinds of variadic binding, distinguished by whether the collected arguments are treated as a list or as a record.&lt;/p&gt;
&lt;h3&gt;List binding with &lt;code&gt;*&lt;/code&gt;&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;list-binding-with-&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#list-binding-with-&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;A &lt;code&gt;*&lt;/code&gt; suffix on a binding name collects a run of positional arguments into a list.
Its position in the binding list determines which arguments it absorbs.
The other bindings around it claim their arguments first, and &lt;code&gt;*&lt;/code&gt; takes the rest:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
first_and_rest := (first, rest*) :: do
  &#39;First: $&#39;.sprintf(first).println
  &#39;Rest: $&#39;.sprintf(rest).println
end

first_and_rest.(1, 2, 3, 4)
# :: First: 1
# :: Rest: [2, 3, 4]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Because position determines what &lt;code&gt;*&lt;/code&gt; absorbs, it can appear anywhere in the binding list:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
(a*, b) := (1, 2, 3, 4)
# a :: [1, 2, 3],  b :: 4

(a, b*) := (1, 2, 3, 4)
# a :: 1,  b :: [2, 3, 4]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The &lt;code&gt;*&lt;/code&gt; message inverts this — it splats a list&amp;rsquo;s values back out into a positional tuple:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
args := [1, 2, 3]
args*   # :: (1, 2, 3)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Together they compose to forward all of a block&amp;rsquo;s arguments, unchanged.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
forward_all := (args*) :: args*
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This is how &lt;code&gt;and:&lt;/code&gt; is implemented for &lt;code&gt;true:&lt;/code&gt; in the core library — it simply returns all its arguments as-is.&lt;/p&gt;
&lt;h3&gt;Dictionary binding with &lt;code&gt;**&lt;/code&gt;&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;dictionary-binding-with-&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#dictionary-binding-with-&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;A &lt;code&gt;**&lt;/code&gt; suffix collects a run of positional arguments and interprets them as &lt;strong&gt;alternating keys and values&lt;/strong&gt;, constructing a record. Like &lt;code&gt;*&lt;/code&gt;, it is positionally aware and can appear anywhere in the binding list:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
(first, kwargs**, last) := (1, name: &#39;bob&#39;, age: 44, 99)
# first  :: 1
# kwargs :: { name: &#39;bob&#39;, age: 44 }
# last   :: 99
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The &lt;code&gt;**&lt;/code&gt; operator on a record is the inverse — it splats a record&amp;rsquo;s key-value pairs back out as a positional sequence of alternating keys and values:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
{ a: &#39;b&#39;, c: &#39;d&#39; } **   # :: (a: &#39;b&#39;, c: &#39;d&#39;)
&lt;/code&gt;&lt;/pre&gt;
&lt;div class=&#34;hx:overflow-x-auto hx:mt-6 hx:flex hx:flex-col hx:rounded-lg hx:border hx:py-4 hx:px-4 hx:border-gray-200 hx:contrast-more:border-current hx:contrast-more:dark:border-current hx:border-blue-200 hx:bg-blue-100 hx:text-blue-900 hx:dark:border-blue-200/30 hx:dark:bg-blue-900/30 hx:dark:text-blue-200&#34;&gt;
  &lt;p class=&#34;hx:flex hx:items-center hx:font-medium&#34;&gt;&lt;svg height=16px class=&#34;hx:inline-block hx:align-middle hx:mr-2&#34; xmlns=&#34;http://www.w3.org/2000/svg&#34; fill=&#34;none&#34; viewBox=&#34;0 0 24 24&#34; stroke-width=&#34;2&#34; stroke=&#34;currentColor&#34; aria-hidden=&#34;true&#34;&gt;&lt;path stroke-linecap=&#34;round&#34; stroke-linejoin=&#34;round&#34; d=&#34;M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z&#34;/&gt;&lt;/svg&gt;Note&lt;/p&gt;

  &lt;div class=&#34;hx:w-full hx:min-w-0 hx:leading-7&#34;&gt;
    &lt;div class=&#34;hx:mt-6 hx:leading-7 hx:first:mt-0&#34;&gt;&lt;p&gt;This message still works on list-like records. The keys will be the integer indices.&lt;/p&gt;&lt;/div&gt;
  &lt;/div&gt;
&lt;/div&gt;
&lt;h3&gt;Keyword-style apis&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;keyword-style-apis&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#keyword-style-apis&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;The &lt;code&gt;**&lt;/code&gt; binding is what makes keyword-argument-style APIs possible in Gab. At the call site, you write alternating message keys and values as positional arguments:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
MyType.some_api(positional, argument, flag: true:)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The definition collects those trailing arguments into a record with &lt;code&gt;**&lt;/code&gt;:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
some_api: .def (MyType, (positional, argument, kwargs**) :: do
  # kwargs :: { flag: true: }
  positional.println
  kwargs.at(flag:).println
end)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;There are no keyword arguments in Gab — only positional ones. The record is constructed from the raw argument sequence by the &lt;code&gt;**&lt;/code&gt; binding. This means you can place a &lt;code&gt;**&lt;/code&gt; binding anywhere, not just at the end, and it will absorb whichever positional arguments fall to it.&lt;/p&gt;
&lt;h2&gt;Tuples and multiple return values&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;tuples-and-multiple-return-values&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#tuples-and-multiple-return-values&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Blocks may return multiple values using a tuple expression like we&amp;rsquo;ve seen before&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
(ok: file.read)     # Two-value tuple
(err: &#39;not found&#39;)  # Two-value tuple
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Receiving multiple return values uses the same tuple syntax on the left side of an assignment:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
(status, file) := IO.File.make(&#39;data.csv&#39;)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Tuple forwarding in chains&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;tuple-forwarding-in-chains&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#tuple-forwarding-in-chains&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;When a message is chained after a call that returns multiple values, Gab forwards the entire tuple into the next send: the first value becomes the receiver, the rest become arguments.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
IO.File.make(&#39;data.csv&#39;).then(file :: file.read.println)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Is exactly equivalent to:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
(status, file) := IO.File.make(&#39;data.csv&#39;)
status.then(() :: file.read.println)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This allows the result of a multi-value return to be routed directly into a &lt;code&gt;defcase&lt;/code&gt;-style dispatch without any intermediate binding.&lt;/p&gt;
&lt;h2&gt;The &lt;code&gt;unwrap:&lt;/code&gt; pattern&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;the-unwrap-pattern&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#the-unwrap-pattern&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;A common use of tuple forwarding is &lt;code&gt;unwrap:&lt;/code&gt;, which either returns the result value or panics with the error:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
unwrap: .defcase {
  ok:  result :: result
  err: msg    :: &#39;Unwrap failed: $&#39;.panicf(msg)
}

file := IO.File.make(&#39;data.csv&#39;).unwrap
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;IO.File.make&lt;/code&gt; returns &lt;code&gt;(ok:, file)&lt;/code&gt; or &lt;code&gt;(err:, message)&lt;/code&gt;. The tuple is forwarded directly to &lt;code&gt;.unwrap&lt;/code&gt;, which dispatches on the first element.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title>Fibers &amp; Channels</title>
      <link>/docs/gabonomicon/basic_types/fibers/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/gabonomicon/basic_types/fibers/</guid>
      <description>
        
        
        &lt;p&gt;For a conceptual introduction to fibers and channels, see &lt;a href=&#34;/docs/gabonomicon/tour/fibers_and_channels/&#34;&gt;the Language Tour&lt;/a&gt;. This page covers the specifics.&lt;/p&gt;
&lt;h2&gt;&lt;code&gt;gab\fiber&lt;/code&gt;&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;gabfiber&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#gabfiber&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Fibers are created with &lt;code&gt;Fibers.make:&lt;/code&gt;, which takes a block and immediately queues it for execution. The fiber may run on any OS thread managed by Gab&amp;rsquo;s runtime.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
Fibers.make () :: do
  &#39;Hello from a fiber!&#39;.println
end
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;&lt;code&gt;gab\channel&lt;/code&gt;&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;gabchannel&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#gabchannel&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Channels are created with &lt;code&gt;Channels.make&lt;/code&gt;:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
ch := Channels.make
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Send&lt;/strong&gt; a value with &lt;code&gt;&amp;lt;!&lt;/code&gt;:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
ch &lt;! &#39;hello&#39;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Receive&lt;/strong&gt; a value with &lt;code&gt;&amp;gt;!&lt;/code&gt;:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
value := ch &gt;!
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;In Gab, channels are unbuffered. Practically, this means that Both operations are &lt;strong&gt;blocking&lt;/strong&gt;: a send blocks until a receiver is ready, and a receive blocks until a sender is ready.
For this reason, channels serve as both a primitive for sharing data, and way to &lt;em&gt;synchronize&lt;/em&gt; fibers.&lt;/p&gt;
&lt;div class=&#34;hx:overflow-x-auto hx:mt-6 hx:flex hx:flex-col hx:rounded-lg hx:border hx:py-4 hx:px-4 hx:border-gray-200 hx:contrast-more:border-current hx:contrast-more:dark:border-current hx:border-blue-200 hx:bg-blue-100 hx:text-blue-900 hx:dark:border-blue-200/30 hx:dark:bg-blue-900/30 hx:dark:text-blue-200&#34;&gt;
  &lt;p class=&#34;hx:flex hx:items-center hx:font-medium&#34;&gt;&lt;svg height=16px class=&#34;hx:inline-block hx:align-middle hx:mr-2&#34; xmlns=&#34;http://www.w3.org/2000/svg&#34; fill=&#34;none&#34; viewBox=&#34;0 0 24 24&#34; stroke-width=&#34;2&#34; stroke=&#34;currentColor&#34; aria-hidden=&#34;true&#34;&gt;&lt;path stroke-linecap=&#34;round&#34; stroke-linejoin=&#34;round&#34; d=&#34;M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z&#34;/&gt;&lt;/svg&gt;Note&lt;/p&gt;

  &lt;div class=&#34;hx:w-full hx:min-w-0 hx:leading-7&#34;&gt;
    &lt;div class=&#34;hx:mt-6 hx:leading-7 hx:first:mt-0&#34;&gt;&lt;p&gt;For a comprehensive example of synchronizing fibers with channels, see the &lt;a href=&#34;/docs/gabonomicon/examples/pubsub/&#34;&gt;pub/sub broker example&lt;/a&gt;.&lt;/p&gt;&lt;/div&gt;
  &lt;/div&gt;
&lt;/div&gt;
&lt;h2&gt;Buffered channels&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;buffered-channels&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#buffered-channels&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;While synchronization is useful, it can sometimes be a bottleneck for performance. If producers outpace consumers, then channels may become backed-up as producers
have to block and wait for consumers.&lt;/p&gt;
&lt;p&gt;In this scenario it is better to use a &lt;em&gt;buffered&lt;/em&gt; channel, which can hold up to a certain number of values before producers have to begin blocking.&lt;/p&gt;
&lt;p&gt;Lets build this with the primitives Gab gives us!&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
buffered: .def (Channels, (n) :: do
  input  := Channels.make
  output := Channels.make

  Ranges.make(0 n).each () :: do
    # Spawn n fibers to perform buffering.
    Fibers.make () :: do
        # Take out of input channel, and forward it to the output channel.
        output &lt;! (input &gt;!)
        # Loop by calling self
        self.()
    end
  end

  (input, output)
end)
&lt;/code&gt;&lt;/pre&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
(input, output) := Channels.buffered(10)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The sender writes to &lt;code&gt;input&lt;/code&gt; and can race N values ahead before blocking. The consumer reads from &lt;code&gt;output&lt;/code&gt;. The N slot fibers sit between them, each capable of holding one value in flight.&lt;/p&gt;
&lt;h2&gt;Note: operator precedence&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;note-operator-precedence&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#note-operator-precedence&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;&lt;code&gt;&amp;lt;!&lt;/code&gt; and &lt;code&gt;&amp;gt;!&lt;/code&gt; are operator sends. The &lt;code&gt;.&lt;/code&gt; prefix is optional but changes the precedence. Named sends (&lt;code&gt;.name&lt;/code&gt;) bind tighter than bare operators.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
ch &lt;! value       # operator form
ch.&gt;! .println    # dot prefix — binds tighter, result chains into .println
&lt;/code&gt;&lt;/pre&gt;

      </description>
    </item>
    
    <item>
      <title></title>
      <link>/docs/modules/atoms/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/modules/atoms/</guid>
      <description>
        
        
        
      </description>
    </item>
    
    <item>
      <title></title>
      <link>/docs/modules/bencode/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/modules/bencode/</guid>
      <description>
        
        
        &lt;h3&gt;to\bencode&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;tobencode&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#tobencode&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
unknown.to\bencode: () :: string
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Convert a value into a bencode string.&lt;/p&gt;
&lt;h3&gt;as\bencode&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;asbencode&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#asbencode&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string.as\bencode: () :: (success (status ok:, value unknown) | failure (status err:, message string))
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Try to parse a string as a bencode value.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title></title>
      <link>/docs/modules/binary/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/modules/binary/</guid>
      <description>
        
        
        &lt;h3&gt;binary&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;binary&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#binary&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
binary
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;A sequence of bytes, with no encoding.&lt;/p&gt;
&lt;h3&gt;len&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;len&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#len&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
binary.len: () :: int
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return the length of the binary, in bytes.&lt;/p&gt;
&lt;h3&gt;as\string&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;asstring&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#asstring&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
binary.as\string: () :: (success (status ok:, value string) | failure (status err:, message string))
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Try to convert the binary to string. This will fail if the binary isn&amp;rsquo;t valid UTF-8.&lt;/p&gt;
&lt;p&gt;This is only attempted once - the result of conversion is cached.&lt;/p&gt;
&lt;h3&gt;at&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;at&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#at&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
binary.at: (default int | stepped (index int, step int)) :: binary
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return the byte at index. If step is supplied, then treat the binary as an array of &amp;lsquo;step&amp;rsquo; size binaries, instead of an array of bytes.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title></title>
      <link>/docs/modules/channel/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/modules/channel/</guid>
      <description>
        
        
        &lt;h3&gt;channel&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;channel&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#channel&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
Channel[predicate\unknown]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Channels allow data to be shared between fibers.&lt;/p&gt;
&lt;h3&gt;close&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;close&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#close&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
channel.close: () :: ()
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Close a channel. Any fibers waiting to receive from this channel will receive &lt;code&gt;none:&lt;/code&gt;.&lt;/p&gt;
&lt;h3&gt;is\closed&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;isclosed&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#isclosed&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
channel.is\closed: () :: boolean
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return &lt;code&gt;true:&lt;/code&gt; if a channel is closed, and &lt;code&gt;false:&lt;/code&gt; otherwise.&lt;/p&gt;
&lt;p&gt;Keep in mind that a channel may be closed by another fiber at any time -
so the return value of this message may change unexpectedly.&lt;/p&gt;
&lt;h3&gt;is\full&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;isfull&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#isfull&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
channel.is\full: () :: boolean
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return &lt;code&gt;true:&lt;/code&gt; if a channel is occupied by a value, and &lt;code&gt;false:&lt;/code&gt; otherwise.&lt;/p&gt;
&lt;p&gt;Keep in mind that a channel may be closed by another fiber at any time -
so the return value of this message may change unexpectedly.&lt;/p&gt;
&lt;h3&gt;is\empty&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;isempty&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#isempty&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
channel.is\empty: () :: boolean
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return &lt;code&gt;true:&lt;/code&gt; if a channel is &lt;em&gt;not&lt;/em&gt; occupied by a value, and &lt;code&gt;false:&lt;/code&gt; otherwise.&lt;/p&gt;
&lt;p&gt;Keep in mind that a channel may be closed by another fiber at any time -
so the return value of this message may change unexpectedly.&lt;/p&gt;
&lt;h3&gt;&amp;lt;!&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;heading&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#heading&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
channel.&lt;!: value unknown :: ()
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Send a value into self. This is always a synchronization point - the sending
fiber &lt;em&gt;will not progress&lt;/em&gt; until the value has been &lt;strong&gt;received&lt;/strong&gt; by another fiber.&lt;/p&gt;
&lt;p&gt;In the case that the channel is closed before a receiver arrives, this message
does nothing.&lt;/p&gt;
&lt;h3&gt;&amp;gt;!&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;heading-1&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#heading-1&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
channel.&gt;!: value unknown :: (some (status ok:, value unknown) | none none:)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Receive a value from self. This is always a synchronization point - the receiving
fiber &lt;em&gt;will not progress&lt;/em&gt; until it has taken a value from another &lt;strong&gt;sending&lt;/strong&gt; fiber.&lt;/p&gt;
&lt;p&gt;In the case that the channel is closed while waiting for a sending fiber, this message
returns &lt;code&gt;none:&lt;/code&gt;.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title></title>
      <link>/docs/modules/dbrow/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/modules/dbrow/</guid>
      <description>
        
        
        &lt;h3&gt;dbrow&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;dbrow&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#dbrow&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
Box[dbow]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;A connection to a row-based database. This implementation wraps sqlite.&lt;/p&gt;
&lt;h3&gt;eval&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;eval&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#eval&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
dbrow.eval: string :: (success ok: | failure (status err:, message string))
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Evaluate the sql string argument against the database &lt;code&gt;self&lt;/code&gt;.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title></title>
      <link>/docs/modules/fiber/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/modules/fiber/</guid>
      <description>
        
        
        &lt;h3&gt;fiber&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;fiber&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#fiber&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
fiber
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Fibers execute gab code.&lt;/p&gt;
&lt;h3&gt;await&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;await&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#await&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
fiber.await: () :: ()
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Blocks the running fiber while self is not done.&lt;/p&gt;
&lt;p&gt;Returns the result of self.&lt;/p&gt;
&lt;h3&gt;is\done&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;isdone&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#isdone&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
fiber.is\done: () :: boolean
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Returns &lt;code&gt;true:&lt;/code&gt; if self has completed execution.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title></title>
      <link>/docs/modules/file/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/modules/file/</guid>
      <description>
        
        
        &lt;h3&gt;file&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;file&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#file&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
Box[io\file]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;A native file handle.&lt;/p&gt;
&lt;h3&gt;make&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;make&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#make&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
io\file:.make: (path string, permissions string) :: file io\file
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Create a file.&lt;/p&gt;
&lt;h3&gt;len&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;len&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#len&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
file.len: () :: int
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Returns the length of a file.&lt;/p&gt;
&lt;h3&gt;stream\send&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;streamsend&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#streamsend&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
streamable.stream\send: bytes binary :: (success (status ok:, value nil:) | failure (status err:, message string))
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Write bytes to a file.&lt;/p&gt;
&lt;p&gt;See the streamable protocol for details.&lt;/p&gt;
&lt;h3&gt;stream\recv&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;streamrecv&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#streamrecv&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
streamable.stream\recv: (n int | default nil:) :: (success (status ok:, value binary) | failure (status err:, message string))
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Read bytes from a file.&lt;/p&gt;
&lt;p&gt;If you omit &amp;lsquo;amount&amp;rsquo;, then will return whatever bytes are immediately available.&lt;/p&gt;
&lt;p&gt;Even for files on disk, &lt;code&gt;recv&lt;/code&gt; with no &lt;code&gt;amount&lt;/code&gt; isn&amp;rsquo;t guaranteed to return the whole file.&lt;/p&gt;
&lt;p&gt;See the streamable protocol for details.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title></title>
      <link>/docs/modules/http/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/modules/http/</guid>
      <description>
        
        
        &lt;h3&gt;to\http&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;tohttp&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#tohttp&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
http\response | http\request.as\http: () :: binary
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Construct a binary http request or response. &lt;code&gt;gab/binary&lt;/code&gt; is the chosen type because the body of a request/response can be an arbitrary encoding, not strictly utf8.&lt;/p&gt;
&lt;h3&gt;as\http&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;ashttp&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#ashttp&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
binary.as\http: () :: (success (status ok:, value http\request | http\response) | failure (status err:, message nil:))
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Attempt to parse a binary as an HTTP request &lt;em&gt;or&lt;/em&gt; response.&lt;/p&gt;
&lt;p&gt;Implemented for binaries, because a request or response body &lt;em&gt;may not be&lt;/em&gt; valid utf8.&lt;/p&gt;
&lt;h3&gt;to\http\code&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;tohttpcode&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#tohttpcode&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
http\status.to\http\code: () :: int
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return the integer status code corresponding to &lt;code&gt;self&lt;/code&gt;.&lt;/p&gt;
&lt;h3&gt;to\http\status&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;tohttpstatus&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#tohttpstatus&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
http\status.to\http\status: () :: string
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return the string representation of the status.&lt;/p&gt;
&lt;h3&gt;request&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;request&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#request&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
[ http\method:, http\endpoint:, http\headers:, http\body: ]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;An HTTP request. See &lt;code&gt;as\http&lt;/code&gt;, as well as &lt;code&gt;method&lt;/code&gt;, &lt;code&gt;endpoint&lt;/code&gt;, &lt;code&gt;headers&lt;/code&gt;, and &lt;code&gt;body&lt;/code&gt;.&lt;/p&gt;
&lt;h3&gt;response&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;response&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#response&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
[ http\status:, http\headers:, http\body: ]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;An HTTP request. See &lt;code&gt;status&lt;/code&gt;, &lt;code&gt;headers&lt;/code&gt;, and &lt;code&gt;body&lt;/code&gt;.&lt;/p&gt;
&lt;h3&gt;status&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;status&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#status&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
http\CONTINUE: | http\SWITCHING_PROTOCOLS: | http\PROCESSING: | http\EARLY_HINTS: | http\RESPONSE_IS_STALE: | http\REVALIDATION_FAILED: | http\DISCONNECTED_OPERATION: | http\HEURISTIC_EXPIRATION: | http\MISCELLANEOUS_WARNING: | http\OK: | http\CREATED: | http\ACCEPTED: | http\NON_AUTHORITATIVE_INFORMATION: | http\NO_CONTENT: | http\RESET_CONTENT: | http\PARTIAL_CONTENT: | http\MULTI_STATUS: | http\ALREADY_REPORTED: | http\TRANSFORMATION_APPLIED: | http\IM_USED: | http\MISCELLANEOUS_PERSISTENT_WARNING: | http\MULTIPLE_CHOICES: | http\MOVED_PERMANENTLY: | http\FOUND: | http\SEE_OTHER: | http\NOT_MODIFIED: | http\USE_PROXY: | http\SWITCH_PROXY: | http\TEMPORARY_REDIRECT: | http\PERMANENT_REDIRECT: | http\BAD_REQUEST: | http\UNAUTHORIZED: | http\PAYMENT_REQUIRED: | http\FORBIDDEN: | http\NOT_FOUND: | http\METHOD_NOT_ALLOWED: | http\NOT_ACCEPTABLE: | http\PROXY_AUTHENTICATION_REQUIRED: | http\REQUEST_TIMEOUT: | http\CONFLICT: | http\GONE: | http\LENGTH_REQUIRED: | http\PRECONDITION_FAILED: | http\PAYLOAD_TOO_LARGE: | http\URI_TOO_LONG: | http\UNSUPPORTED_MEDIA_TYPE: | http\RANGE_NOT_SATISFIABLE: | http\EXPECTATION_FAILED: | http\IM_A_TEAPOT: | http\PAGE_EXPIRED: | http\ENHANCE_YOUR_CALM: | http\MISDIRECTED_REQUEST: | http\UNPROCESSABLE_ENTITY: | http\LOCKED: | http\FAILED_DEPENDENCY: | http\TOO_EARLY: | http\UPGRADE_REQUIRED: | http\PRECONDITION_REQUIRED: | http\TOO_MANY_REQUESTS: | http\REQUEST_HEADER_FIELDS_TOO_LARGE_UNOFFICIAL: | http\REQUEST_HEADER_FIELDS_TOO_LARGE: | http\LOGIN_TIMEOUT: | http\NO_RESPONSE: | http\RETRY_WITH: | http\BLOCKED_BY_PARENTAL_CONTROL: | http\UNAVAILABLE_FOR_LEGAL_REASONS: | http\CLIENT_CLOSED_LOAD_BALANCED_REQUEST: | http\INVALID_X_FORWARDED_FOR: | http\REQUEST_HEADER_TOO_LARGE: | http\SSL_CERTIFICATE_ERROR: | http\SSL_CERTIFICATE_REQUIRED: | http\HTTP_REQUEST_SENT_TO_HTTPS_PORT: | http\INVALID_TOKEN: | http\CLIENT_CLOSED_REQUEST: | http\INTERNAL_SERVER_ERROR: | http\NOT_IMPLEMENTED: | http\BAD_GATEWAY: | http\SERVICE_UNAVAILABLE: | http\GATEWAY_TIMEOUT: | http\HTTP_VERSION_NOT_SUPPORTED: | http\VARIANT_ALSO_NEGOTIATES: | http\INSUFFICIENT_STORAGE: | http\LOOP_DETECTED: | http\BANDWIDTH_LIMIT_EXCEEDED: | http\NOT_EXTENDED: | http\NETWORK_AUTHENTICATION_REQUIRED: | http\WEB_SERVER_UNKNOWN_ERROR: | http\WEB_SERVER_IS_DOWN: | http\CONNECTION_TIMEOUT: | http\ORIGIN_IS_UNREACHABLE: | http\TIMEOUT_OCCURED: | http\SSL_HANDSHAKE_FAILED: | http\INVALID_SSL_CERTIFICATE: | http\RAILGUN_ERROR: | http\SITE_IS_OVERLOADED: | http\SITE_IS_FROZEN: | http\IDENTITY_PROVIDER_AUTHENTICATION_ERROR: | http\NETWORK_READ_TIMEOUT: | http\NETWORK_CONNECT_TIMEOUT:
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;An HTTP status code. These are defined as &lt;code&gt;http\&amp;lt;HTTP STATUS&amp;gt;&lt;/code&gt;.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
http\OK:        # The &#39;200 OK&#39; status.
http\NOT_FOUND: # The &#39;404 NOT FOUND&#39; status.
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;method&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;method&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#method&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
DELETE: | GET: | HEAD: | POST: | PUT: | CONNECT: | OPTIONS: | TRACE: | COPY: | LOCK: | MKCOL: | PROPFIND: | PROPPATCH: | SEARCH: | UNLOCK: | BIND: | REBIND: | ACL: | REPORT: | MKACTIVITY: | CHECKOUT: | MERGE: | MSEARCH: | NOTIFY: | SUBSCRIBE: | UNSUBSCRIBE: | PATCH: | PURGE: | MKCALENDAR: | LINK: | UNLINK: | SOURCE: | PRI: | DESCRIBE: | ANNOUNCE: | SETUP: | PLAY: | PAUSE: | TEARDOWN: | GET_PARAMETER: | SET_PARAMETER: | REDIRECT: | RECORD: | FLUSH: | QUERY:
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;An HTTP method, as a message.&lt;/p&gt;
&lt;h3&gt;endpoint&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;endpoint&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#endpoint&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;An HTTP path.&lt;/p&gt;
&lt;h3&gt;headers&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;headers&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#headers&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
Dict[string, string]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;A record of HTTP header -&amp;gt; value.&lt;/p&gt;
&lt;p&gt;HTTP headers must contain only ASCII characters. For this reason, we choose the string type over binary.&lt;/p&gt;
&lt;h3&gt;body&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;body&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#body&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
binary
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The body of an HTTP request or response. Opaque bytes.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title></title>
      <link>/docs/modules/json/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/modules/json/</guid>
      <description>
        
        
        &lt;h3&gt;to\json&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;tojson&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#tojson&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
unknown.to\json: () :: string
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Convert a value into a json string.&lt;/p&gt;
&lt;h3&gt;as\json&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;asjson&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#asjson&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string.as\json: () :: (success (status ok:, value unknown) | failure (status err:, message string))
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Try to parse a string as a json value.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title></title>
      <link>/docs/modules/number/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/modules/number/</guid>
      <description>
        
        
        &lt;h3&gt;number&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;number&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#number&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
float
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;A 64-bit floating number.&lt;/p&gt;
&lt;h3&gt;float\between&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;floatbetween&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#floatbetween&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
gab\number:.float\between: (() | from 0 to  float | between (lower float, upper float)) :: number
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return a random float between lower and upper..&lt;/p&gt;
&lt;h3&gt;Pi&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;pi&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#pi&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
gab\number:.Pi: () :: number
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return pi.&lt;/p&gt;
&lt;h3&gt;E&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;e&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#e&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
gab\number:.E: () :: number
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return e.&lt;/p&gt;
&lt;h3&gt;Infinity&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;infinity&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#infinity&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
gab\number:.Infinity: () :: number
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return infinity.&lt;/p&gt;
&lt;h3&gt;MaxInt&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;maxint&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#maxint&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
gab\number:.MaxInt: () :: number
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return the maximum safe integer available to the gab runtime.&lt;/p&gt;
&lt;h3&gt;is\nan&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;isnan&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#isnan&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
number.is\nan: () :: boolean
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Returns true if self is NaN.&lt;/p&gt;
&lt;h3&gt;is\inf&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;isinf&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#isinf&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
number.is\inf: () :: boolean
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Returns true if self is infinity.&lt;/p&gt;
&lt;h3&gt;floor&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;floor&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#floor&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
number.floor: () :: number
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Returns the largest integral value not greater than self.&lt;/p&gt;
&lt;h3&gt;ceil&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;ceil&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#ceil&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
number.ceil: () :: number
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Returns the smallest integral value not less than self.&lt;/p&gt;
&lt;h3&gt;round&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;round&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#round&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
number.is\round: () :: number
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Returns self to the nearest integral value.&lt;/p&gt;
&lt;h3&gt;acos&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;acos&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#acos&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
number.acos: () :: number
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Returns the arccosine of self.&lt;/p&gt;
&lt;h3&gt;asin&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;asin&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#asin&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
number.asin: () :: number
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Returns the arcsine of self.&lt;/p&gt;
&lt;h3&gt;atan&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;atan&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#atan&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
number.atan: () :: number
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Returns the arctangent of self.&lt;/p&gt;
&lt;h3&gt;cos&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;cos&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#cos&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
number.cos: () :: number
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Returns the cosine of self.&lt;/p&gt;
&lt;h3&gt;sin&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;sin&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#sin&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
number.sin: () :: number
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Returns the sine of self.&lt;/p&gt;
&lt;h3&gt;tan&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;tan&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#tan&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
number.tan: () :: number
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Returns the tangent of self.&lt;/p&gt;
&lt;h3&gt;abs&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;abs&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#abs&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
number.abs: () :: number
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Returns the absolute value of self.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title></title>
      <link>/docs/modules/record/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/modules/record/</guid>
      <description>
        
        
        &lt;h3&gt;record&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;record&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#record&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
record
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;An immutable set of key,value pairs. This is the core datastructure of the language.&lt;/p&gt;
&lt;p&gt;In addition to the builtin messages defined in this module, records also respond to their &lt;em&gt;keys&lt;/em&gt; as messages.&lt;/p&gt;
&lt;h3&gt;len&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;len&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#len&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
record.len: () :: int
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return the number of key-value pairs in the record.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
[1 2 3].len # :: 3
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;at&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;at&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#at&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
record.at: unknown :: (some (status ok:, value unknown) | none none:)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Check for a value at the given key.&lt;/p&gt;
&lt;p&gt;Returns a tuple with the result of the check, and the value if it was found.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
(ok value) := { name: &#39;John Doe&#39; } .at (name:)

ok    # :: ok:
value # :: &#39;John Doe&#39;

(ok value) := { name: &#39;John Doe&#39; } .at (age:)

ok    # :: none:
value # :: nil:
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;has&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;has&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#has&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
record.has: unknown :: boolean
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Check if the receiver has the given key. Returns true: if it does, and false otherwise.&lt;/p&gt;
&lt;div class=&#34;hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code&#34;&gt;

&lt;div&gt;&lt;pre&gt;&lt;code&gt;

### put
```gab
record.put: (key unknown, value unknown) :: record&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;div class=&#34;hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0&#34;&gt;
  &lt;button
    class=&#34;hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50&#34;
    title=&#34;Copy code&#34;
    aria-label=&#34;Copy code&#34;
    data-copied-label=&#34;Copied!&#34;
  &gt;
    &lt;div class=&#34;hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
&lt;div class=&#34;hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
  &lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;Return a new record with the additional key-value pair, or an updated value for an existing key.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
person := { name: &#39;John Doe&#39; }

person := person.put(age: 30)

person.name # :: &#39;John Doe&#39;
person.age  # :: 30
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;put_via&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;put_via&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#put_via&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
record.put_via: (key_path *unknown, value unknown) :: record
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Traverse nested records with the given &lt;code&gt;key_path&lt;/code&gt;, eventually performing a final put.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
person := {
  mother: := {
    name: &#39;Jane Doe&#39; 
  }
}

person := person.put_via(mother: age: 60)

person.mother.age # :: 60
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This message will create records along the path where none exist.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
{}.put_via(mother: sister: daugher: relation: &#39;cousin&#39;)

# :: { mother: { sister: { daughter: { relation: &#39;cousin&#39; } } } }
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;at_via&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;at_via&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#at_via&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
record.at_via: key_path *unknown :: (some (status ok:, value unknown) | none none:)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Get a value by traversing nested maps with the given &lt;code&gt;key_path&lt;/code&gt;.
If any key along the path is missing, returns &lt;code&gt;none:&lt;/code&gt;&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
(ok, value) := record.at_via(a: long: run: of: keys:)
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;put_by&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;put_by&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#put_by&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
record.put_by: (key unknown, f is\block:) :: record
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Apply a function to the value at key, returning a new record with the returned value from that function at key.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
person := { age: 30 }

person := person.put_by(age: age :: age + 1)

person.age # :: 30
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;If the key does not exist in the record, then the argument to the block will be &lt;code&gt;nil:&lt;/code&gt;.&lt;/p&gt;
&lt;h3&gt;put_via_by&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;put_via_by&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#put_via_by&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
record.put_via_by: (key_path *unknown, f is\block:) :: record
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Apply a function to the value at &lt;code&gt;key_path&lt;/code&gt;, returning a new record with the returned value from that function at key_path.&lt;/p&gt;
&lt;p&gt;Combines the behavior of &lt;code&gt;put_via:&lt;/code&gt; and &lt;code&gt;put_by:&lt;/code&gt;.&lt;/p&gt;
&lt;h3&gt;push&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;push&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#push&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
record.push: unknown :: record
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return a new record with the given value at the &lt;em&gt;end&lt;/em&gt; of the record. The key will be the length of the record &lt;em&gt;before&lt;/em&gt; inserting.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
arr := [1 2 3] .push 4

arr # :: [1 2 3 4]
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;pop&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;pop&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#pop&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
record.pop: () :: (some (status ok:, value (record record, popped_value unknown, popped_key unknown)) | none none:)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return a new record without the &lt;em&gt;last&lt;/em&gt; key-value pair in the record.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;

(ok rec val key) := [1 2 3 4].pop

ok  # :: ok:
rec # :: [1 2 3]
val # :: 4
key # :: 3
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;If the record was empty, returns &lt;code&gt;none:&lt;/code&gt; and an empty record.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
[].pop # :: none: []
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;is\record&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;isrecord&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#isrecord&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
unknown.is\record: () :: boolean
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Returns &lt;code&gt;true:&lt;/code&gt; if the receiver is a record, and &lt;code&gt;false:&lt;/code&gt; otherwise.&lt;/p&gt;
&lt;h3&gt;is\list&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;islist&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#islist&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
record.is\list: () :: boolean
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Returns &lt;code&gt;true:&lt;/code&gt; if the receiving record is a list, and false otherwise.&lt;/p&gt;
&lt;h3&gt;is\dict&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;isdict&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#isdict&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
record.is\dict: () :: boolean
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Returns &lt;code&gt;true:&lt;/code&gt; if the receiving record is a dict, and false otherwise.&lt;/p&gt;
&lt;h3&gt;keys&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;keys&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#keys&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
record.keys: () :: List[unknown]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return a list of all the keys in the record, in order.&lt;/p&gt;
&lt;h3&gt;vals&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;vals&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#vals&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
record.vals: () :: List[unknown]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return a list of all the values in the record, in order.&lt;/p&gt;
&lt;h3&gt;plus&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;plus&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#plus&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
record.+: other record :: record
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Concatenate the argment record onto self.&lt;/p&gt;
&lt;h3&gt;slice&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;slice&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#slice&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
record.slice: (end int | both (begin int, end int)) :: record
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return a new record with only the key-value pairs in the range
(0, end) or (begin, end).&lt;/p&gt;
&lt;h3&gt;&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
record.: unknown :: unknown
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return the value at the key given by the argument. If the key doesn&amp;rsquo;t exist, panic.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
map := { &#39;id&#39; 1234 }

map.(&#39;id&#39;) # :: 1234

map.(&#39;name&#39;) # :: panic
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This allows for records to serve as functions of their keys.&lt;/p&gt;
&lt;h3&gt;take&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;take&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#take&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
record.take: unknown :: (remaining record, value unknown)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Given a key, return a tuple of a new record &lt;em&gt;without&lt;/em&gt; the given key, and whatever value was found at that key (if any).&lt;/p&gt;
&lt;h3&gt;seq\init&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;seqinit&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#seqinit&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
seqable.seq\init: () :: (next (status ok:, key unknown, values *unknown) | done none:)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Begin iterating the key-value pairs in the record.&lt;/p&gt;
&lt;p&gt;See the seqable protocol for details.&lt;/p&gt;
&lt;h3&gt;seq\next&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;seqnext&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#seqnext&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
seqable.seq\next: key unknown :: (next (status ok:, key unknown, values *unknown) | done none:)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Continue iterating the key-value pairs in the record.&lt;/p&gt;
&lt;p&gt;See the seqable protocol for details.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title></title>
      <link>/docs/modules/shape/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/modules/shape/</guid>
      <description>
        
        
        &lt;h3&gt;shape&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;shape&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#shape&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
shape
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;An immutable set of keys.&lt;/p&gt;
&lt;h3&gt;push&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;push&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#push&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
shape.push: unknown :: shape
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Push additional keys to the end of a shape.&lt;/p&gt;
&lt;h3&gt;pop&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;pop&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#pop&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
shape.pop: unknown :: remaining shape
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Pop a key off the back of the shape&lt;/p&gt;
&lt;h3&gt;len&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;len&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#len&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
shape.len: unknown :: int
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return the numbers of keys in the shape.&lt;/p&gt;
&lt;h3&gt;seq\init&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;seqinit&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#seqinit&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
seqable.seq\init: () :: (next (status ok:, key unknown, values *unknown) | done none:)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Begin iterating the keys in the shape.&lt;/p&gt;
&lt;p&gt;See the seqable protocol for details.&lt;/p&gt;
&lt;h3&gt;seq\next&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;seqnext&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#seqnext&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
seqable.seq\next: key unknown :: (next (status ok:, key unknown, values *unknown) | done none:)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Continue iterating the keys in the shape.&lt;/p&gt;
&lt;p&gt;See the seqable protocol for details.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title></title>
      <link>/docs/modules/socket/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/modules/socket/</guid>
      <description>
        
        
        &lt;h3&gt;socket&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;socket&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#socket&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
Box[io\socket]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;A native socket handle.&lt;/p&gt;
&lt;p&gt;Implements the &lt;code&gt;streamable&lt;/code&gt; protocol.&lt;/p&gt;
&lt;p&gt;When creating a socket, the user must choose a &lt;code&gt;protocol&lt;/code&gt;. The options are:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;tcp:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;udp:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;tcp\tls:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;udp\tls:&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The latter two include transport-layer-security. Sockets with and without &lt;code&gt;tls:&lt;/code&gt; require different
arguments to messages such as &lt;code&gt;connect:&lt;/code&gt; or &lt;code&gt;bind:&lt;/code&gt;. See their definitions for details.&lt;/p&gt;
&lt;h3&gt;make&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;make&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#make&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
io\socket:.make: protocol (TCP tcp: | UDP udp: | TCP with TLS tcp\tls: | UDP with TLS udp\tls:) :: socket io\socket
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Create a socket with the given protocol.&lt;/p&gt;
&lt;h3&gt;accept&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;accept&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#accept&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
socket.accept: () :: (success (status ok:, value io\socket) | failure (status err:, message string))
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Accept a client connection on a listening server socket.&lt;/p&gt;
&lt;h3&gt;listen&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;listen&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#listen&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
socket.listen: max_connections int :: (ok ok: | err err:)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Begin listening for connections on a server socket.&lt;/p&gt;
&lt;h3&gt;bind&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;bind&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#bind&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
socket.bind: (default (address string, port int) | tls (address string, port int, certificate binary, private_key binary)) :: (success (status ok:, value nil:) | failure (status err:, message string))
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Bind a socket to a local address. This turns a socket into a server socket.&lt;/p&gt;
&lt;h3&gt;connect&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;connect&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#connect&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
socket.connect: (default (address string, port int) | tls (address string, port int, certificate binary)) :: (success (status ok:, value nil:) | failure (status err:, message string))
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Connect to an address. This turns a socket into a client socket.&lt;/p&gt;
&lt;p&gt;For ssl clients, Gab bundles a public mozilla client certificate chain, so the &amp;lsquo;certificate&amp;rsquo;
argument is optional.&lt;/p&gt;
&lt;h3&gt;stream\send&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;streamsend&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#streamsend&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
streamable.stream\send: bytes binary :: (success (status ok:, value nil:) | failure (status err:, message string))
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Write bytes to a socket.&lt;/p&gt;
&lt;p&gt;This is part of the &lt;code&gt;streamable&lt;/code&gt; protocool.&lt;/p&gt;
&lt;h3&gt;stream\recv&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;streamrecv&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#streamrecv&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
streamable.stream\recv: (n int | default nil:) :: (success (status ok:, value binary) | failure (status err:, message string))
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Read &amp;lsquo;amount&amp;rsquo; bytes from a socket.&lt;/p&gt;
&lt;p&gt;If you omit &amp;lsquo;amount&amp;rsquo;, then will return whatever bytes are immediately available.&lt;/p&gt;
&lt;p&gt;This is part of the &lt;code&gt;streamable&lt;/code&gt; protocool.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title></title>
      <link>/docs/modules/string/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/modules/string/</guid>
      <description>
        
        
        &lt;h3&gt;string&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;string&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#string&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;A valid UTF-8 encoded sequence of bytes.&lt;/p&gt;
&lt;h3&gt;is\blank&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;isblank&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#isblank&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string.is\blank: () :: boolean
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Returns &lt;code&gt;true:&lt;/code&gt; if the string contains only blank characters. Returns &lt;code&gt;false:&lt;/code&gt; otherwise.&lt;/p&gt;
&lt;h3&gt;split&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;split&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#split&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string.split: delimiter string :: tokens *string
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Split the receiving string on the given delimitier.&lt;/p&gt;
&lt;h3&gt;has\sub&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;hassub&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#hassub&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string.has\sub: substring string :: boolean
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Returns &lt;code&gt;true:&lt;/code&gt; if the receiver contains the argument string.&lt;/p&gt;
&lt;h3&gt;has\suffix&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;hassuffix&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#hassuffix&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string.has\suffix: substring string :: boolean
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Returns &lt;code&gt;true:&lt;/code&gt; if the receiver ends with the argument string.&lt;/p&gt;
&lt;h3&gt;has\prefix&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;hasprefix&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#hasprefix&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string.has\prefix: substring string :: boolean
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Returns &lt;code&gt;true&lt;/code&gt; if the receiver string begins with the argument string.&lt;/p&gt;
&lt;h3&gt;to\message&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;tomessage&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#tomessage&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string.to\message: () :: message
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Convert a string into a message.&lt;/p&gt;
&lt;h3&gt;to\binary&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;tobinary&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#tobinary&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string.to\binary: () :: binary
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Convert a string into a binary.&lt;/p&gt;
&lt;h3&gt;as\number&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;asnumber&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#asnumber&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string.as\number: () :: (success (status ok:, value float) | failure (status err:, message nil:))
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Try to convert a string into a number. If successful, return &lt;code&gt;ok:&lt;/code&gt; and the number. Otherwise, return &lt;code&gt;err:&lt;/code&gt;.&lt;/p&gt;
&lt;h3&gt;len&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;len&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#len&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string.len: () :: int
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return the length of the string. This does not count the bytes of the string - it returns the number of graphemes.&lt;/p&gt;
&lt;h3&gt;at&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;at&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#at&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string.at: index int :: (some (status ok:, value string) | none none:)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return &lt;code&gt;ok:&lt;/code&gt; and the grapheme at the given index. If none exists, return &lt;code&gt;none:&lt;/code&gt;.&lt;/p&gt;
&lt;h3&gt;slice&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;slice&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#slice&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string.slice: (begin (some (status ok:, value int) | none none:), end (some (status ok:, value int) | none none:)) :: string
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return a substring of graphemes at the given indices. Will panic if indices are out of bounds.&lt;/p&gt;
&lt;h3&gt;pop&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;pop&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#pop&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string.pop: () :: (some (status ok:, value (rest string, last string)) | none none:)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Similar behavior to &lt;code&gt;pop:&lt;/code&gt; on the records.&lt;/p&gt;
&lt;h3&gt;trim&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;trim&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#trim&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string.trim: trimset string :: string
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Trim graphemes from the front and back of the string if they are within the arg &amp;rsquo;trimset&#39;.&lt;/p&gt;
&lt;h3&gt;seq\init&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;seqinit&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#seqinit&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
seqable.seq\init: () :: (next (status ok:, key unknown, values *unknown) | done none:)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Begin iterating the graphemes in the string.&lt;/p&gt;
&lt;p&gt;See the seqable protocol for details.&lt;/p&gt;
&lt;h3&gt;seq\next&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;seqnext&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#seqnext&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
seqable.seq\next: key unknown :: (next (status ok:, key unknown, values *unknown) | done none:)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Continue iterating the graphemes in the string.&lt;/p&gt;
&lt;p&gt;See the seqable protocol for details.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title></title>
      <link>/docs/modules/ui/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/modules/ui/</guid>
      <description>
        
        
        &lt;h3&gt;run&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;run&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#run&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
ui:.run: (platform gui: | tui:, events Channel, frames Channel) :: ()
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Begin running an application.&lt;/p&gt;
&lt;p&gt;The platform argument defines how the application renders.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;gui:&lt;/code&gt; Render in a new, native operating system window&lt;/li&gt;
&lt;li&gt;&lt;code&gt;tui:&lt;/code&gt; Render in the terminal&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The &lt;em&gt;events&lt;/em&gt; and &lt;em&gt;frames&lt;/em&gt; arguments are the entry/exit points for interacting with the running app.&lt;/p&gt;
&lt;p&gt;Every frame, a new event will arrive on the &lt;em&gt;events&lt;/em&gt; channel. For more on events, see &lt;code&gt;ui\event&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;It is the program&amp;rsquo;s task to pull events off the &lt;code&gt;events&lt;/code&gt; channel, process them, and put corresponding views onto the &lt;code&gt;frames&lt;/code&gt; channel.&lt;/p&gt;
&lt;p&gt;A &lt;em&gt;view&lt;/em&gt; is a data structure which describes the layout of the app. The &lt;code&gt;ui&lt;/code&gt; library takes each view off the &lt;code&gt;frames&lt;/code&gt; channel, walks the
data structure, and renders a new frame. The cycle then repeats.&lt;/p&gt;
&lt;p&gt;For more information on views, see &lt;code&gt;ui\view&lt;/code&gt;.&lt;/p&gt;
&lt;h3&gt;event&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;event&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#event&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
predicate:alt:{ mouse predicate:cat:{ kind mouse, button predicate:any:[ left, right ]:, target predicate:message: }:, key predicate:cat:{ kind key, key predicate\string:, val predicate\int:, pressed predicate\boolean: }:, tick predicate:cat:{ kind tick, tick tick, hovered predicate:message: }: }:
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;A &lt;em&gt;ui\event&lt;/em&gt; is a tuple of values describing some user interaction, or simply the passage of time.&lt;/p&gt;
&lt;h3&gt;component&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;component&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#component&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
predicate:alt:{ text predicate:pat:{ kind text:, opts predicate\record: }:, rect predicate:pat:{ kind rect:, opts predicate\record: }:, img predicate:pat:{ kind img:, opts predicate\record: }:, box predicate:pat:{ kind box:, opts predicate\record:, children ui\view: }: }:
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;A component is UI element, drawable to the screen.&lt;/p&gt;
&lt;h3&gt;view&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;view&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#view&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
List[ui\component:]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;A list of UI components.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title></title>
      <link>/docs/modules/uri/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/modules/uri/</guid>
      <description>
        
        
        &lt;h3&gt;uri&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;uri&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#uri&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
[ uri\scheme:, uri\authority:, uri\path:, uri\query:, uri\fragment: ]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;A fully-parsed uri. See &lt;code&gt;scheme&lt;/code&gt;, &lt;code&gt;authority&lt;/code&gt;, &lt;code&gt;path&lt;/code&gt;, &lt;code&gt;query&lt;/code&gt;, and &lt;code&gt;fragment&lt;/code&gt; for more details on these components.&lt;/p&gt;
&lt;h3&gt;as\uri&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;asuri&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#asuri&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string.as\uri: () :: (success (status ok:, value [ uri\scheme:, uri\authority:, uri\path:, uri\query:, uri\fragment: ]) | failure (status err:, message string))
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Parse a string as a URI.&lt;/p&gt;
&lt;p&gt;Returns an error if parsing fails. This can happen if the URI is poorly formed, or if percent-decoding fails.
For details on percent-decoding, see &lt;code&gt;as\uri\encoded&lt;/code&gt;.&lt;/p&gt;
&lt;h3&gt;to\uri&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;touri&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#touri&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
uri.to\uri: () :: string
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Convert a uri into its string form. Performs percent-encoding on each component.&lt;/p&gt;
&lt;p&gt;Neither the uri conversion nor the percent-encoding can fail in this direction.&lt;/p&gt;
&lt;h3&gt;to\uri\encoded&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;touriencoded&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#touriencoded&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string.to\uri\encoded: () :: string
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;URI-encodes the string.&lt;/p&gt;
&lt;p&gt;URI encoding is necessary as some characters are &lt;em&gt;reserved&lt;/em&gt; in URIs, such as &lt;code&gt;/&lt;/code&gt;, &lt;code&gt;@&lt;/code&gt;, &lt;code&gt;:&lt;/code&gt; and others, which carry structural meaning within a URI.&lt;/p&gt;
&lt;p&gt;Percent encoding allows these characters to appear in segments of a URI by including a &lt;code&gt;%&lt;/code&gt; character, followed by a two-character hexadecimal corresponding to the
ASCII value of the percent-encoded character. Here is a simple example:&lt;/p&gt;
&lt;div class=&#34;hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code&#34;&gt;

&lt;div&gt;&lt;pre&gt;&lt;code&gt;/path with spaces/a/b/c&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;div class=&#34;hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0&#34;&gt;
  &lt;button
    class=&#34;hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50&#34;
    title=&#34;Copy code&#34;
    aria-label=&#34;Copy code&#34;
    data-copied-label=&#34;Copied!&#34;
  &gt;
    &lt;div class=&#34;hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
&lt;div class=&#34;hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
  &lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;Spaces and other whitespace/control-characters must be encoded when they appear as data within a URI. The above URI will &lt;code&gt;uri\encode&lt;/code&gt; as:&lt;/p&gt;
&lt;div class=&#34;hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code&#34;&gt;

&lt;div&gt;&lt;pre&gt;&lt;code&gt;/path%20with%20spaces/a/b/c&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;div class=&#34;hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0&#34;&gt;
  &lt;button
    class=&#34;hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50&#34;
    title=&#34;Copy code&#34;
    aria-label=&#34;Copy code&#34;
    data-copied-label=&#34;Copied!&#34;
  &gt;
    &lt;div class=&#34;hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
&lt;div class=&#34;hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
  &lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;The &lt;code&gt; &lt;/code&gt; characters are replaced with the percent encoding &lt;code&gt;%20&lt;/code&gt; (A &lt;code&gt;%&lt;/code&gt;, followed by the ASCII value of a space, &lt;code&gt;20&lt;/code&gt;).&lt;/p&gt;
&lt;h3&gt;as\uri\encoded&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;asuriencoded&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#asuriencoded&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string.as\uri\encoded: () :: (success (status ok:, value string) | failure (status err:, message string))
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;URI-decodes a string.&lt;/p&gt;
&lt;p&gt;Will fail if an invalid percent-encoded value is encountered.&lt;/p&gt;
&lt;p&gt;An invalid percent-encoded value is defined as a &lt;code&gt;%&lt;/code&gt; followed by two non-hexadecimal characters.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
&#39;%20&#39;.as\uri\encoded # :: (ok: &#39; &#39;)
&#39;%zz&#39;.as\uri\encoded # :: (err: &#39;Invalid hexadecimal value&#39;)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The &lt;code&gt;z&lt;/code&gt; character cannot represent a hexadecimal value (valid characters are 0-9, a-f, A-F).&lt;/p&gt;
&lt;p&gt;If there are fewer than two characters after the &lt;code&gt;%&lt;/code&gt;, decoding will also fail.&lt;/p&gt;
&lt;h3&gt;scheme&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;scheme&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#scheme&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;A URI scheme. Common examples are &lt;code&gt;http&lt;/code&gt; or &lt;code&gt;file&lt;/code&gt;.&lt;/p&gt;
&lt;div class=&#34;hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code&#34;&gt;

&lt;div&gt;&lt;pre&gt;&lt;code&gt;https://me:psk@abc.com:8080/path/to/somewhere?sort=true#whatever
^^^^^&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;div class=&#34;hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0&#34;&gt;
  &lt;button
    class=&#34;hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50&#34;
    title=&#34;Copy code&#34;
    aria-label=&#34;Copy code&#34;
    data-copied-label=&#34;Copied!&#34;
  &gt;
    &lt;div class=&#34;hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
&lt;div class=&#34;hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
  &lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;h3&gt;authority&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;authority&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#authority&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
[ uri\host:, uri\port:, uri\username:, uri\password: ]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The authority portion of a URI can have up to four components:&lt;/p&gt;
&lt;div class=&#34;hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code&#34;&gt;

&lt;div&gt;&lt;pre&gt;&lt;code&gt;https://me:psk@abc.com:8080/path/to/somewhere?sort=true#whatever
        ^^^^^^^^^^^^^^^^^^^&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;div class=&#34;hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0&#34;&gt;
  &lt;button
    class=&#34;hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50&#34;
    title=&#34;Copy code&#34;
    aria-label=&#34;Copy code&#34;
    data-copied-label=&#34;Copied!&#34;
  &gt;
    &lt;div class=&#34;hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
&lt;div class=&#34;hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
  &lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;uri\authority is a record containing any combination of these keys.
See &lt;code&gt;host&lt;/code&gt;, &lt;code&gt;port&lt;/code&gt;, &lt;code&gt;username&lt;/code&gt;, and &lt;code&gt;password&lt;/code&gt;.&lt;/p&gt;
&lt;h3&gt;path&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;path&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#path&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
List[string]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;A list of segments from the path portion of a URI.&lt;/p&gt;
&lt;p&gt;Each segment is separated by a &amp;lsquo;/&amp;rsquo; character.&lt;/p&gt;
&lt;div class=&#34;hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code&#34;&gt;

&lt;div&gt;&lt;pre&gt;&lt;code&gt;https://me:psk@abc.com:8080/path/to/somewhere?sort=true#whatever
                            ^^^^^^^^^^^^^^^^^&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;div class=&#34;hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0&#34;&gt;
  &lt;button
    class=&#34;hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50&#34;
    title=&#34;Copy code&#34;
    aria-label=&#34;Copy code&#34;
    data-copied-label=&#34;Copied!&#34;
  &gt;
    &lt;div class=&#34;hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
&lt;div class=&#34;hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
  &lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;h3&gt;query&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;query&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#query&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
Dict[string, string]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;A record of the query parameters from the URI.&lt;/p&gt;
&lt;div class=&#34;hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code&#34;&gt;

&lt;div&gt;&lt;pre&gt;&lt;code&gt;https://me:psk@abc.com:8080/path/to/somewhere?sort=true&amp;amp;new=false#whatever
                                              ^^^^^^^^^^^^^^^^^^^&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;div class=&#34;hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0&#34;&gt;
  &lt;button
    class=&#34;hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50&#34;
    title=&#34;Copy code&#34;
    aria-label=&#34;Copy code&#34;
    data-copied-label=&#34;Copied!&#34;
  &gt;
    &lt;div class=&#34;hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
&lt;div class=&#34;hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
  &lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;This record will contain the key-value present in the URI. Both keys and values are strings.&lt;/p&gt;
&lt;h3&gt;fragment&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;fragment&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#fragment&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;A URI fragment.&lt;/p&gt;
&lt;div class=&#34;hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code&#34;&gt;

&lt;div&gt;&lt;pre&gt;&lt;code&gt;https://me:psk@abc.com:8080/path/to/somewhere?sort=true&amp;amp;new=false#whatever
                                                                  ^^^^^^^^&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;div class=&#34;hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0&#34;&gt;
  &lt;button
    class=&#34;hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50&#34;
    title=&#34;Copy code&#34;
    aria-label=&#34;Copy code&#34;
    data-copied-label=&#34;Copied!&#34;
  &gt;
    &lt;div class=&#34;hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
&lt;div class=&#34;hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
  &lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;h3&gt;host&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;host&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#host&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The host in the uri authority, if present.&lt;/p&gt;
&lt;div class=&#34;hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code&#34;&gt;

&lt;div&gt;&lt;pre&gt;&lt;code&gt;https://me:psk@abc.com:8080/path/to/somewhere?sort=true#whatever
               ^^^^^^^&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;div class=&#34;hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0&#34;&gt;
  &lt;button
    class=&#34;hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50&#34;
    title=&#34;Copy code&#34;
    aria-label=&#34;Copy code&#34;
    data-copied-label=&#34;Copied!&#34;
  &gt;
    &lt;div class=&#34;hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
&lt;div class=&#34;hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
  &lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;h3&gt;port&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;port&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#port&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The port in the uri authority, if present.&lt;/p&gt;
&lt;div class=&#34;hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code&#34;&gt;

&lt;div&gt;&lt;pre&gt;&lt;code&gt;https://me:psk@abc.com:8080/path/to/somewhere?sort=true#whatever
                       ^^^^&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;div class=&#34;hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0&#34;&gt;
  &lt;button
    class=&#34;hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50&#34;
    title=&#34;Copy code&#34;
    aria-label=&#34;Copy code&#34;
    data-copied-label=&#34;Copied!&#34;
  &gt;
    &lt;div class=&#34;hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
&lt;div class=&#34;hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
  &lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;h3&gt;username&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;username&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#username&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The username in the uri authority, if present.&lt;/p&gt;
&lt;div class=&#34;hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code&#34;&gt;

&lt;div&gt;&lt;pre&gt;&lt;code&gt;https://me:psk@abc.com:8080/path/to/somewhere?sort=true#whatever
        ^^&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;div class=&#34;hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0&#34;&gt;
  &lt;button
    class=&#34;hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50&#34;
    title=&#34;Copy code&#34;
    aria-label=&#34;Copy code&#34;
    data-copied-label=&#34;Copied!&#34;
  &gt;
    &lt;div class=&#34;hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
&lt;div class=&#34;hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
  &lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;h3&gt;password&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;password&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#password&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
string
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The password in the uri authority, if present.&lt;/p&gt;
&lt;div class=&#34;hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code&#34;&gt;

&lt;div&gt;&lt;pre&gt;&lt;code&gt;https://me:psk@abc.com:8080/path/to/somewhere?sort=true#whatever
           ^^^&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;div class=&#34;hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0&#34;&gt;
  &lt;button
    class=&#34;hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50&#34;
    title=&#34;Copy code&#34;
    aria-label=&#34;Copy code&#34;
    data-copied-label=&#34;Copied!&#34;
  &gt;
    &lt;div class=&#34;hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
&lt;div class=&#34;hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4&#34;&gt;&lt;/div&gt;
  &lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;

      </description>
    </item>
    
    <item>
      <title></title>
      <link>/docs/protocols/reduceable/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/protocols/reduceable/</guid>
      <description>
        
        
        &lt;h3&gt;reduceable&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;reduceable&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#reduceable&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
[ reduce\step:, reduce\done: ]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The &lt;code&gt;reduceable&lt;/code&gt; protocol allows a value to serve as a reducing function.&lt;/p&gt;
&lt;p&gt;Send &lt;code&gt;reduce\step:&lt;/code&gt; to a reduceable, with an accumulator and any values:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
  (cmd r acc) := reduceable.reduce\step(acc, args)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Reducers can terminate a sequence before the seqable runs out of values.
This is determined by the cmd, which can be &lt;code&gt;next:&lt;/code&gt; or &lt;code&gt;stop:&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;The second return value &lt;code&gt;r&lt;/code&gt; is a new reduceable to use for the next step of the sequence. This is because some reduceables are &lt;em&gt;stateful&lt;/em&gt;, and need to change as values arrive over the sequence.&lt;/p&gt;
&lt;p&gt;For example, something like &lt;code&gt;drop:&lt;/code&gt; needs to keep track of the number of values it has dropped.&lt;/p&gt;
&lt;p&gt;The third return value &lt;code&gt;acc&lt;/code&gt; is the new value for the accumulator. This is standard for reducing.&lt;/p&gt;
&lt;p&gt;Once a reduceable is done (ie: the stream has returned &lt;code&gt;none:&lt;/code&gt;), send &lt;code&gt;reduce\done:&lt;/code&gt; in case the reduceable has any work it needs to do in order to finalize the accumulated value.&lt;/p&gt;
&lt;p&gt;In many cases, &lt;code&gt;reduce\done:&lt;/code&gt; just forwards the accumulator as it received it.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
  acc := reduceable.reduce\done acc
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;With these two messages, it is possible to define a message which recursively process a seqable with a reduceable.&lt;/p&gt;
&lt;p&gt;In this example, &lt;code&gt;seq\i&lt;/code&gt; and &lt;code&gt;seq\v&lt;/code&gt; are used as naming conventions for the sequence&amp;rsquo;s invariant and variant respectively.&lt;/p&gt;
&lt;p&gt;The &lt;em&gt;invariant&lt;/em&gt; is the value which &lt;strong&gt;doesn&amp;rsquo;t&lt;/strong&gt; change over the course of iterating the sequence. This is typically the seqable itself.&lt;/p&gt;
&lt;p&gt;The &lt;em&gt;variant&lt;/em&gt; is the value which &lt;strong&gt;does&lt;/strong&gt; change over the course of iteration. This is the &lt;code&gt;key&lt;/code&gt; as described in the &lt;code&gt;seqable&lt;/code&gt; module.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
  doreduce: .defcase {
    # The sequence continues
    ok: (seq\i, seq\v, acc, r, xs) :: do
      # Apply the reducer
      (cmd, r, acc) := r.reduce\step(acc, xs*)
      # proceed to the next sep
      cmd.doreduce(seq\i, seq\v, acc, r)
    end

    # The sequence is done
    none: (seq\i, seq\v, acc, r) :: do
      r.reduce\done(acc)
    end

    # The reducer continues
    next: (seq\i, seq\v, acc, r) :: do
      # Apply the producer resolving new values for the next step
      (ok, seq\v, xs*) := seq\i .seq\next seq\v
      # Tailcall to the next step
      ok.doreduce(seq\i, seq\v, acc, r, xs)
    end

    # The reducer is done
    stop: (seq\i, seq\v, acc, r) :: do
      r.reduce\done(acc)
    end
  }

  # Initialize the seq
  (ok, seq\v, xs*) := self.seq\init
  # Tail recursive algorithm with `doreduce:`
  ok.doreduce(self, seq\v, acc, r, xs)
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;reduce\step&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;reducestep&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#reducestep&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
reduceable.reduce\step: (accumulator unknown, sequence_args *unknown) :: (command next: | stop:, reducer [ reduce\step:, reduce\done: ], accumulator unknown)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Performs one step of reducing work.&lt;/p&gt;
&lt;h3&gt;reduce\done&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;reducedone&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#reducedone&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
reduceable.reduce\done: accumulator unknown :: accumulator unknown
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Finish reducing work. Perform any final computation the reducer needs to do in order to produce a final accumulated value.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title></title>
      <link>/docs/protocols/seqable/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/protocols/seqable/</guid>
      <description>
        
        
        &lt;h3&gt;seqable&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;seqable&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#seqable&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
[ seq\init:, seq\next: ]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The &lt;code&gt;seqable&lt;/code&gt; protocol defines how a value can produce a lazy sequence of other values.&lt;/p&gt;
&lt;p&gt;To use the &lt;code&gt;seqable&lt;/code&gt; protocol, begin by sending &lt;code&gt;seq\init&lt;/code&gt;:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
  (ok next values*) := [&#39;1&#39; &#39;2&#39; &#39;3&#39; &#39;4&#39;].seq\init
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;em&gt;ok&lt;/em&gt; will contain &lt;code&gt;ok:&lt;/code&gt; or &lt;code&gt;none:&lt;/code&gt;. This determines whether or not the sequence had an initial value.&lt;/p&gt;
&lt;p&gt;It follows that empty sequences return &lt;code&gt;none:&lt;/code&gt; when &lt;code&gt;seq\init&lt;/code&gt; is sent.&lt;/p&gt;
&lt;p&gt;Non-empty sequences will return &lt;code&gt;ok:&lt;/code&gt;, followed by &lt;em&gt;next&lt;/em&gt; (more on that later), followed by a run of values.&lt;/p&gt;
&lt;p&gt;The run of values can be any number of values. Records return &lt;code&gt;(value, key)&lt;/code&gt;, so for the above example:&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
  ok     # :: ok:
  next   # :: 1
  values # :: [&#39;1&#39;, 0] (values collects the tuple into a list)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The &lt;em&gt;next&lt;/em&gt; value is a key which the user can pass to &lt;code&gt;seq\next:&lt;/code&gt; to receive the &lt;em&gt;next&lt;/em&gt; iteration in the sequence.&lt;/p&gt;
&lt;p&gt;This is what makes the sequence lazy - the user computes the next value only when they need it.&lt;/p&gt;

&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
  (ok next values*) := [&#39;1&#39; &#39;2&#39; &#39;3&#39; &#39;4&#39;].seq\next 1

  ok     # :: ok: 
  next   # :: 2
  values # :: [&#39;2&#39;, 1]
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;seq\init&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;seqinit&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#seqinit&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
seqable.seq\init: () :: (next (status ok:, key unknown, values *unknown) | done none:)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Return the initial key and value which should begin the sequence.&lt;/p&gt;
&lt;h3&gt;seq\next&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;seqnext&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#seqnext&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
seqable.seq\next: key unknown :: (next (status ok:, key unknown, values *unknown) | done none:)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Given a key, return the next keys and values which would continue the sequence.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title></title>
      <link>/docs/protocols/streamable/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/protocols/streamable/</guid>
      <description>
        
        
        &lt;h3&gt;streamable&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;streamable&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#streamable&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
[ stream\send:, stream\recv: ]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The &lt;code&gt;streamable&lt;/code&gt; protocol defines an interface for a value to stream bytes in or out as a &lt;strong&gt;side effect&lt;/strong&gt;.&lt;/p&gt;
&lt;p&gt;A file handle or socket connection are examples of devices which could serve as &lt;code&gt;streamable&lt;/code&gt;.&lt;/p&gt;
&lt;h3&gt;stream\send&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;streamsend&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#streamsend&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
streamable.stream\send: bytes binary :: (success (status ok:, value nil:) | failure (status err:, message string))
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Send the binary argument through the device. This API is synchronous (IE, the operation is guranteed to be completed when this message returns.)&lt;/p&gt;
&lt;p&gt;Returns &lt;code&gt;ok:&lt;/code&gt; on success, or &lt;code&gt;err:&lt;/code&gt; if an error occurred.&lt;/p&gt;
&lt;h3&gt;stream\recv&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;streamrecv&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#streamrecv&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
streamable.stream\recv: (n int | default nil:) :: (success (status ok:, value binary) | failure (status err:, message string))
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;When &lt;code&gt;arg&lt;/code&gt; is not &lt;code&gt;nil:&lt;/code&gt;, perform a blocking wait for &lt;code&gt;arg&lt;/code&gt; bytes on the device, and return them. Otherwise, don&amp;rsquo;t block and return as many bytes as are available.&lt;/p&gt;

      </description>
    </item>
    
    <item>
      <title></title>
      <link>/docs/protocols/transduceable/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>/docs/protocols/transduceable/</guid>
      <description>
        
        
        &lt;h3&gt;transduceable&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;transduceable&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#transduceable&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
[ transduce\wrap:, transduce\step: ]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The &lt;code&gt;transduceable&lt;/code&gt; protocol defines how a value serves as a &lt;em&gt;transducer&lt;/em&gt;.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Reduce&lt;/em&gt; means &lt;em&gt;to lead back&lt;/em&gt;.
Think of a reducer as a function which &lt;em&gt;leads&lt;/em&gt; a sequence of values back towards a single value.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Transduce&lt;/em&gt; is an invented word (by Rich Hickey afaik), and the root &lt;em&gt;trans&lt;/em&gt; means &lt;em&gt;across&lt;/em&gt;,
changing the meaning of the word to &lt;em&gt;to bring across&lt;/em&gt;. Think of a transducer as leading a
sequence of values &lt;em&gt;across&lt;/em&gt; or &lt;em&gt;through&lt;/em&gt; some transformation.&lt;/p&gt;
&lt;p&gt;Therefore, a transducer&amp;rsquo;s job is to &lt;em&gt;wrap&lt;/em&gt; a reducer, and manipulate the values passed &lt;em&gt;through&lt;/em&gt;
to the reducer, without it ever having to know.&lt;/p&gt;
&lt;p&gt;The fundamental utility of a transducer is that it defines &lt;em&gt;when a reducing function is called&lt;/em&gt;.&lt;/p&gt;
&lt;p&gt;For example, the &lt;code&gt;filter:&lt;/code&gt; transducer ensures that the reducing function is only called when
values meet a certain condition.&lt;/p&gt;
&lt;p&gt;To accomplish this, a transduceable must implement two messages, &lt;code&gt;wrap&lt;/code&gt; and &lt;code&gt;step&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;transduce\wrap:&lt;/code&gt; wraps the argument &lt;em&gt;reducing function&lt;/em&gt; with the &lt;code&gt;self&lt;/code&gt; transducer.&lt;/p&gt;
&lt;p&gt;This gives the transducer somewhere to &lt;em&gt;send&lt;/em&gt; the values it brings across.&lt;/p&gt;
&lt;p&gt;In order to transduce values, the transducer should implement &lt;code&gt;transduce\step:&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;This is conceptually similar to &lt;code&gt;reduce\step:&lt;/code&gt; for the &lt;code&gt;reduceable:&lt;/code&gt; protocol, but has a different signature.&lt;/p&gt;
&lt;p&gt;In addition to receiving an accumulator and tuple of values from the sequence, &lt;code&gt;transduce\step:&lt;/code&gt; receives
a &lt;em&gt;reduceable&lt;/em&gt; and a &lt;em&gt;state&lt;/em&gt; argument as well.&lt;/p&gt;
&lt;h3&gt;transduce\wrap&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;transducewrap&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#transducewrap&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
transduceable.transduce\wrap: reducer reduceable: :: reduceable:
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Wrap a &lt;code&gt;reduceable&lt;/code&gt; with some behavior, returning a new &lt;code&gt;reduceable&lt;/code&gt;.&lt;/p&gt;
&lt;h3&gt;transduce\step&lt;span class=&#34;hx:absolute hx:-mt-20&#34; id=&#34;transducestep&#34;&gt;&lt;/span&gt;
    &lt;a href=&#34;#transducestep&#34; class=&#34;subheading-anchor&#34; aria-label=&#34;Permalink for this section&#34;&gt;&lt;/a&gt;&lt;/h3&gt;
&lt;!--gab--&gt;&lt;pre&gt;&lt;code&gt;
transduceable.transduce\step: (reducer reduceable:, accumulator unknown, sequence List[unknown], state unknown) :: (state unknown, command next: | stop:, reducer reduceable:, accumulator unknown)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Step the transducer. This may call the wrapped &lt;code&gt;reduceable&lt;/code&gt; any number of times per step.&lt;/p&gt;

      </description>
    </item>
    
  </channel>
</rss>
